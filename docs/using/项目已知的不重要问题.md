# 项目已知的不重要问题

> 2026-09-18 建立，2026-09-24 更名并按 `Saved/RuntimeTelemetry/20260924-164759-BP_IG_Character_C_0-55480/` 复核。仅列仍存在或本次未覆盖、尚不能排除的条目；各节开头的“最新复核”表示当前证据，后续旧录制保留作历史对照。
>
> 旧记录以 Monster Hunter Rise 实机录制为对照，本项目侧引用 `Saved/RuntimeTelemetry/*/` 的逐帧 CSV 与 `Saved/Logs/MHGZ.log` 的 `[VaultDbg]` 行。新旧录制不得混作同一版本的现状。

---

## 1. 突刺入口短暂停步（仍存在，暂缓）

**最新复核（2026-09-24）**：这次录制有 3 次 `AM_IG_TuCi`，`Spatial.csv` 均采到连续两个近零水平速度样本，随后恢复：帧 `1636～1639` 为 `396 → 0.2 → 1.4 → 441 cm/s`；`2761～2764` 为 `425 → 0.1 → 4.3 → 442 cm/s`；`4275～4278` 为 `405 → 0.1 → 0 → 432 cm/s`。同段胶囊 XY 坐标连续，没有突然跳位。`Spatial` 在 CMC 更新外采样，与 `MovementPhases` 的同号帧相差一个更新相位，不能把这两个样本直接说成“CMC 连续两帧都在静止保持段”。例如帧 `1636/1637` 的 `CMC.PerformMovement.Post` 仍分别只有约 `0.2/1.4 cm/s`，帧 `1638` 已恢复至约 `441 cm/s`。旧版结论仍适用于这个入口，但这份 CSV 不能替代玩家对顿挫是否明显的目视判断。处理优先级仍按用户决定暂缓。

2026-09-23 的项目录制中，红灯突刺 `AM_IG_TuCi` 某次进入时，胶囊水平速度从约 `424 cm/s` 降到近零两帧，随后恢复；低速帧位于无根位移的 `AS_IG_TuCi_EntryHold`。Rise 的红灯突刺 `118` 入口没有同样的归零。这里记录的是**速度和一小段时间内的位移差异**，角色位置连续，没有观察到模型瞬移。之前提到的“约 15 cm 位移被推迟”只是按不同入口速度估算的位置差，不能据此断言后面补回了同一段位移。

用户此前没有感到明显的模型变化，决定先不修。蒙太奇、GA、Combo 和通知窗口均未为此改动；此前用于试验入口根运动的内存自动化测试已撤销。此项不再列入空回 P0/P1 修复方案。

### 1a. 同族的入口硬零：突进回旋斩与地面前翻滚（2026-09-24 复跑，证据归位）

第 1 条**不是突刺独有**。以「蒙太奇起始帧起 4 帧内 `v2D ≤ 5`」为口径扫 69 个遥测会话：

| 蒙太奇 | 命中 / 实例 | 备注 |
|---|---|---|
| `AM_IG_TuJinHuiXuan` 突进回旋斩 | **193 / 193 = 100%** | 回弹只到 ~125 cm/s，所以按「1–2 帧内回到 ≥150」的严格签名会被漏掉 |
| `AM_IG_TuCi` 突刺 | **466 / 469 = 99%** | 回弹 1–2 帧内到 ≥150 —— 只有它读起来像一次顿挫 |
| `AM_IG_ChongYin` / `AM_IG_HengSao` | 4/4、4/4 | 小样本 |
| `AM_IG_Dodge_Forward` 地面前翻滚 | **4 / 11** | 形状与突刺同级：`440.3 → 460.0 → **0.0** → 660.7 → 1058.0` |
| 对照**不塌** | `AM_IG_AirDodge` 0/163、`AM_Shth_Dodge` 0/12、猎虫三条 0/107 | |

**资产侧**（逐段解析 `.uasset` tagged property）：14 个地面攻击蒙太奇里 **8 个首段就是合成的 `*EntryHold`**（ChongYin / FeiShenYueRu / HengSao / HouChe / JiaSha / ShangLao / TuCi / TuJinHuiXuan；**TuCi 是两个背靠背 = 0.1 s 连续零根**），另 2 个把 hold 放在中/尾（FeiYuan 0.05 s@2.90833、SiLianYinZhan 0.05 s@0.53333），4 个完全没有 hold（BaDao / DrawSlash / ErLian / WuTa）；**地面四向翻滚 `AM_IG_Dodge_*` 的首段同为 `*_EntryHold_Slot0`（0.05 s）**。
⚠ 15 个 hold 里**只有 TuCi 的两个与 Dodge 的两个**在仓库里有实测 `RootDelta=(0,0,0)`（E4.2 行 82/84 与 38/61），其余 13 个的零根**是推断**；且 E4.2 用的 `ExtractRootTrackTransform` 已被仓库自己的 `Scripts/AssetProbe/probe_m5_assets.py:242-253` 标记为「会骗人」（对 Jump_Left/Right 恒返回 0）。

机制与第 1 条同链：Walking 下 `ConstrainAnimRootMotionVelocity` 是**全量替换**（仅 Falling 保 Z，`CharacterMovementComponent.cpp:2115`），所以「新蒙太奇自己的保持段没有根、旧根已停」这一帧里胶囊就没有第二个人来维持速度。

**用户 2026-09-24 口径**：`_W` = **白灯**变体；红灯只影响部分攻击动作；**在用的攻击蒙太奇全是红灯的** ⇒ 上表条目都在用、**在范围内**（不是「记录就完」）。`AM_IG_TuCi` 挂 118 `AS_Unsh_R_TuCi`（强化突刺）而非 104，与该口径一致，判定为**有意**。

⚠ 折扣：语料是**确定性输入回放**（三个会话存在逐位相同的速度轨迹），466 / 193 应读作「录制事件数」而非独立按键次数。

---

## 2. 无白灯后撑杆跳（仍存在）

### 2a. 下坠交接处一帧卡顿（仍存在）

**最新复核（2026-09-24）**：无白灯后撑杆跳 `HouChengGanTiao_Over → AerialFall` 的帧 `1733`，胶囊实际下降 `3.51 cm`，按当前 `VZ=-1114.7 cm/s` 与本帧时长预计约 `27.78 cm`，相差 `24.27 cm`；水平速度维持约 `302.5 cm/s`。上一帧根运动源 `CurrentTime=1.0802 < Duration=1.0833`，交接帧源已不在列表。另 3 次无白灯前撑杆跳同类交接帧 `1545/1977/2209` 分别少下降 `26.28/25.85/28.12 cm`。这次仍能测到交接帧竖直位移缺口，数值不同于 9 月 18 日旧录制；没有据此断言玩家必能看到顿挫。

以下是 2026-09-17～18 的历史分析，具体帧值不代表当前版本：

**用户原话**：「顿一下指的是**下坠**的时候，而不是上升的时候，大概是 JumpOver 切 Fall 的时候」。

**实测坐实**：CurvedVault 源的**末帧之后那一帧**，下坠量从约 −28 cm 掉到约 −9 cm，再恢复：

| | 20260918-205813（当时版本） | 20260917-210929（重构前） |
|---|---|---|
| 源末帧 `dZ` | −27.91 | −27.77 |
| **交接帧 `dZ`** | **−9.73** | **−8.61** |
| 次帧 `dZ` | −29.43 | −28.39 |
| 交接帧 `dVZ` | **0.0** | **0.0** |

**两版逐帧一致 ⇒ 既有。** `Velocity.Z` 全程连续（−1114.7），所以不是速度跳变，是**那一帧的位移被截断**，一帧约丢 17~19 cm —— 所以「不明显」。

**已排除**：碰撞（整段飞行只有落地那一次胶囊命中，法线 Z=1）；采样假象（那段 0.1 s 内 Z 实降 −100.9 cm，而 `VZ` 积分预测 −118.2 cm，缺 17 cm 是真实的）。

**机制（已定位到引擎行）**。`FinishVelocityParams` 的安装点是 `FRootMotionSourceGroup::CleanUpInvalidRootMotion`（`RootMotionSource.cpp:1346`，装速度在 `:1381-1401`），由 `UCharacterMovementComponent::PerformMovement` 在开头调用（`CharacterMovementComponent.cpp:2743-2753`）。**帧内顺序是 `CleanUp → PrepareRootMotion → StartNewPhysics`。** 于是卡顿那一帧：

1. 帧开始时源**还没**被判完成（遥测末帧 `cT=1.075 < Duration=1.083`），`CleanUp` 不动它 ⇒ 切线速度**没装上**；
2. 紧接着 `PrepareRootMotion` 把时间推过 `Duration`、标 `Finished`，而它只产出路径**剩下的那 0.4%**（进度 0.8767 → 0.8808）⇒ 这一帧的位移就是这点残量；
3. 到**下一帧**的 `CleanUp` 才移除源并装上 `SetVelocity` 切线 ⇒ 从下一帧起恢复 −29.43。

**一句话：源过期的那个部分帧只走了路径残量，而切线速度要等下一帧才装得上，中间正好丢一帧。**

**注意 `ApplyCurvedVaultSource` 已经做对了**（切线在建源时就装进 `FinishVelocityParams`，注释写着「removes the frame-order dependency entirely」）—— 那句话在**值**上成立，在**帧**上不成立。

**历史修法设想（未实施，也未在现版本验证）**：当时考虑将源到期与切线速度安装放到同一更新中处理。地面攻击是否同源，需要独立录制证明，不能从这 4 次撑杆跳交接推断所有 movement task 都如此。

**顺带**：这段下坠 cT 0.725→1.076 在两版之间**逐帧数值相同**，而旧版 `MovementMode` 到末帧才由 5 变 3、新版早在 0.80 就变 3 ⇒ **换模式对下坠轨迹没有任何影响**，两个积分器的差异在这条弧上测不出来。

### 2b. 上升段起跳后暂时不升高（仍存在）

**最新复核（2026-09-24）**：这次无白灯后撑杆跳从帧 `1663` 起播，胶囊 `Z=98.15 cm` 保持至 `1677`，帧 `1678` 才首次上升至 `102.56 cm`；两采样间隔合计约 `0.378 s`，该段 `MovementMode=5`、`VelocityZ=0`。但旧文所述 `Z=196.7 cm` 附近 `VZ 452.7→0` 的交棒塌速没有复现：帧 `1689` 为 `Z=196.7 cm, VZ=397.0 cm/s`，帧 `1690` 升至 `Z=235.4 cm, VZ=1555.3 cm/s`。本次只有 1 次无白灯后撑杆跳，不能给未复现的塌速下“已修复”结论。

**当前代码线索**：

- 现行 `BeginBackVaultInitialFlight`（`Source/MHGZ/ActionSystem/MHGZPoleVaultAbility.cpp:652`）会先将胶囊置于 `MOVE_Flying`；蒙太奇尚未产出 Z 位移的那段时间不会由重力推动上升。
- 现行交棒仍由 `UAbilityTask_WaitDelay` 按 `JumpDuration - VaultHandoffLeadSeconds` 计时（同文件约 `:823-826`），不是由蒙太奇 section 边界通知直接驱动。旧版文件名与行号已失效。

**修的时候注意**：`Mode == BallisticVault` 的 `[VaultDbg] tick` 守卫（`AbilityTask_MHGZWeaponMovement.cpp:184`）**不覆盖 CurvedVault**，所以这段只能靠 `Saved/RuntimeTelemetry/*/Character/Spatial.csv` 的 `MovementMode` 列看。

---

## 3. 突进回旋斩反击：触发延迟与「先受击再起飞」（本次未触发，待复核）

**最新复核（2026-09-24）**：录制有 6 次 `TuJinHuiXuan → WuTa`，角色 `Health` 全程为 `100`，未录到受击蒙太奇。CSV 缺少火环每次提交命中的精确时刻，无法据此测出反击从命中到起舞的延迟；“先受击再起飞”在这次录制中没有触发。下面的因果解释只代表旧版调查，不能作为本次 PIE 已复现的结论。

**现象**：按下反击后到真正起舞有明显延迟；有时先看到受击动画，然后才起飞。

**反击链本身没有延迟源**。已逐行核实：从 `HandleIncomingHit`（`MHGZAdvancingCounterAbility.cpp:218-248`）到 `ApplyBallisticVaultSource` 是**同一帧内的同步调用**，中间没有计时器、没有蒙太奇段边界、没有 suspend/resume 交接。唯一的门是 `AM_IG_TuJinHuiXuan` 里 `IG_AdvancingCounter` 那条 notify 窗口在蒙太奇时间轴上的位置。

**延迟来自训练桩**：`MHGZTrainingDummy` 的火环按 `HitInterval = 0.25 s` 提交命中（`Source/MHGZ/Monster/MHGZDummyConfig.h:121`），环本身 `ActiveDuration = 1.0`、`EmissionInterval = 5.0`（`:113`/`:117`）。所以反击只有在某次环击**恰好落进 notify 窗口**时才能触发 —— 最多等 0.25 s；窗口整个错过则要等下一个环（最多 5 s）。

**「先受击再起飞」是同一个采样的两面**：落在窗口外的环击不被消耗 → 走伤害路径 → `Combat.Event.HitStagger` → `UMHGZHitReactionAbility` → `CancelActivePlayerActions()`；下一次落在窗口内的环击被 Consume → 起身。日志 `Saved/Logs/MHGZ.log` 里能直接看到这个交替。

> **这条要改得动火环节奏（测试桩）或补空中受击表现，属于测试夹具问题，不是战斗逻辑问题。**

---

## 4. 空中没有专用受击表现（代码仍未实现，本次未触发）

**最新复核（2026-09-24）**：本次 `Health=100` 全程不变，没有空中受击样本。现行 `UMHGZHitReactionAbility::ActivateAbility` 仍只选择四向 `Light` 地面受击序列，没有按空中状态选专用表现；中/大硬直也没有代播小硬直。该缺口由代码确认，本次录制不能验证实际空中命中的观感。

- 起跳后**不允许收刀**（设计如此）。
- 被空中命中时播的是**地面**受击动画。`UMHGZHitReactionAbility` 目前只有 `Combat.Stagger.Light` 一条表现路径（`Source/MHGZ/ActionSystem/MHGZHitReactionAbility.cpp:162-166`），素材取自 `AS_Unsh_Hited_Little_*`（`:54-61`）。
- **尚未制作**空中专用的受击动画与该分支的接线。

---

## 5. 落地结束后进入 Idle、Walk 时姿态抽搐（未修，暂缓）

**现象**：落地动作播完后推摇杆，角色经短暂 Idle 再进入 Walk，能感觉到姿态抽搐。用户在 `slomo 0.1` 下观察到落地尾帧、Idle、Walk 起步之间的姿势和脚步相位不够连续；调长 ABP 或蒙太奇的混合时间没有消除这种感觉。

**已有定位**：此前录制的路径是 `落地蒙太奇 → Idle 约 2～3 帧 → Walk_Start@0 → Walk_Loop`。现行落地期间会强制 `bForceMMIdle`，尚无落地专用的相位匹配路由。混合可以缓和姿势变化，但不能保证 `Walk_Start@0` 与落地尾帧的脚步相位接上。

**证据边界**：`20260924-164759` 的 CSV 记录了落地和移动状态，但没有逐骨姿势，不能仅凭速度或胶囊位移证明这次视觉抽搐的具体帧。此项与撑杆跳下落交接的竖直位移缺口、落地时的 `337 cm/s` 速度重设是不同问题。按既定计划暂缓，之后需结合落地尾帧与 MM 选片、脚步相位及用户目视复核。

---

## 6. 前向翻滚、移动收刀的 MoveExit → 普通移动衔接（待复核）

**此前 PIE 观察**：收刀前翻滚和移动收刀在持续推摇杆时，Walk/MoveExit 播放数帧后会短暂停步，再继续走；`slomo 0.1` 下动作仍连续，但约有一帧没有实际位移。持刀前翻滚的 MoveExit 接 Walk Loop 时，左右脚的位置或步态相位也显得不顺。这里把**位移短暂停顿**与**姿态/脚步接缝**分开记录，尚不能认定两者有同一个原因。

**涉及路径**：[E4.2 动作退出路由](../editor/e4.2-action-exit-routing.md) 中的 `AM_Shth_Dodge → DodgeMoveExit`、`AM_IG_Dodge_Forward → DodgeMoveExit` 和移动中的 `AM_IG_ShouDao → SheatheMoveExit`。持刀后、左、右翻滚没有 MoveExit，不在此项范围内。

**证据边界**：E4.2/M4.5 文档曾记录这三条路径完成接线和当时的 PIE 验收，但不能据此排除用户之后观察到的衔接问题。本次 `20260924-164759` 录制未对这三条地面退出路径逐一做定向复现，因此当前版本是否仍有停顿、各路径分别出现哪一种接缝，都待正常速度与 `slomo 0.1` 的 PIE 复核。复核时分别对照 Exit 选片和播放时间、胶囊逐帧位移，以及 Exit 尾帧与 Walk Loop 的左右脚相位；此项不与第 5 条落地后 Idle → Walk 抽搐合并。
