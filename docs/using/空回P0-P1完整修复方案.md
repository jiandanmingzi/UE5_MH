# 空回 P0 / P1 完整修复方案

> 2026-09-23。本文定义修复次序与验收口径，执行状态见下文。初始失败基线为 `20260923-151654-BP_IG_Character_C_0-55480`，当前回退版基线为 `20260923-200039-BP_IG_Character_C_0-55482`；逐帧证据见 [空回坠落顿挫与项目现状审查](空回坠落顿挫与项目现状审查.md)。Monster Hunter Rise 真值以脚本生成的 `docs/reference/下落物理.md` 为准，不直接手改真值文件。

## 执行进度（2026-09-23）

### 24:00：P0/P1 逐条审计（用户问「其他 P0/P1 修了吗」；录制 `20260923-235744` 目测+遥测双通过）

用户判定「目测已无问题」，遥测四条判据全过（缝上交叉帧 / 157 出现 / 风格 tag / 姿势不再冻结）。
借此把本表五条逐条核一遍（代码 + 自动化 + PIE 数据）：

| 条目 | 状态 | 依据 |
|---|---|---|
| **P0-1** | **症状已消，机制未修** | 空回缝上 `vXY` **7/7 恒 900**（改前 4~11 次/会话塌到 0），下坠段保持 900（7 帧走 157.3 cm = 22.5 cm/帧）—— 与真值「137→157→落地是同一条抛物线、无速度重置」一致。原因：缝那一帧槽被填满（保持终态的空回淡出 + 157 淡入）⇒ 下层图不再漏近零根位移 ⇒ `PerformMovement:2870` 那一帧不可达。**但写点本身没修**（窄门控按用户要求已删）⇒ 机制在别处仍触发：同一份录制里 `AM_IG_TuCi` 有 1 帧 424 → 0。按本表「在该点修正」的定义，**未满足** |
| **P0-2** | **未满足** | `MHGZ.M5.Aerial.AirDodgeToFallHandoffKeepsPlanarVelocityWithoutCollision` 装配是真链路，但①交接由测试接缝在第 26 帧摆出（`SetAirDodgeExitFlagsForTest` + `HandleAirDodgeVisualCompletedForTest`，正是本方案明令禁止的形态）；②测试世界不 tick、AnimInstance 是基础 `UAnimInstance`（无动画图）；③**表现资产的根运动抽取在段级就是关的**（`MHGZStripPresentationRootMotion` + 资产钉 `PresentationClipsHaveNoRootMotion`）⇒ `bHasRootMotion` 恒假、引擎 `CharacterMovementComponent.cpp:2870` 不可达 ⇒ **该测试在缺陷存在与否都通过**。全仓库没有任何一条会在 P0-1 存在时变红的自动化。⚠ 由此推出一条：今天要构造「旧版本红」得**同时钉代码修订与资产修订**，因为刺激物已从资产里剥掉 |
| **P1-1** | **部分满足** | 写点齐（`PlayAerialLandingVisual` 回调写值 + `QueuePostMovementLandingSpeedReset`/`ApplyPendingLandingSpeedReset` 帧末一次性重写；337、方向保持、近零回退朝向、Z 不动、非每帧锁速），且有**真触地**两档数据：约 900（`235744` 7/7、`225916` 11 次）与约 300（`225916` f1441/f3811 = 298.6/296.4、`194246` f2883 = 298.6），写后均为 337.0。缺：自动化仍是「手设速度 + 直接调表现入口」的函数内形态（无真实 `LandedDelegate`）；「下一帧值」零覆盖；**「与 Rise 148 首帧相同采样相位」只有散文**（三处注释 + 两处待办），没落成断言或遥测列 —— 而这一项非空：实测落地**首采样帧**读到 900、**次帧**才 337，撑杆跳那档却是首帧就 337 ⇒ 相位不定就说不清 337±6 是哪一个点 |
| **P1-2** | **未实现（用户决定推迟）** | 无 `EMHGZMotionMatchingHandoffType::LandingExit`、`HandleAerialLandingMontageEnded` 不发布任何 MM 载荷、AnimInstance 无落地专用路由；文档要求「只对该 handoff 关闭」的 2 帧输入 settle 与 0.05 s 输出保持都没被绕过 ⇒ 仍是「落地 → Idle 2~3 帧 → `Walk_Start@0`」的原始断链。三份文档一致标暂缓 |
| **P1-3** | **已交付** | 子阶段记录层 + 遥测列；本轮两次都是靠它定名（任务的 tick 门、缝上的钳制帧） |

**顺带排除一条疑似回归**：本轮录制里撑杆跳「起手段→弧段」首帧出现 `vXY` 掉到 ~0（如 f2050: 74.7 → 0.3，
`vZ` 456 → 1.4）。核过：那是 **Flying + 尚无根运动源** 的减速滑行签名（`ActiveRootMotionSourceCount` 0 → 1），
在 `204414/204736/130710/224312/141138/151654/163739/175313/200039/210402/214103` 等**十余个更早的会话**里
同样出现（各会话 0~37% 的首帧），与本轮改动无关（共享任务新开的 tick 在 `bCompleteAtMontageEnd` 为假时是空操作）。

### 23:00：撑杆跳配方第一次 PIE —— 缝上不塌了，但**下坠段姿势冻结**（已修，一行）

用户判定：「除了下坠段看起来没有播放动画（动作是对的）之外没有任何问题」。
**姿势空档那条确实修好了**（缝上不再是 −17.8→−33.5 的下陷回弹）；但遥测暴露一个更大的空档：
空回蒙太奇播到自身长度后**保持终末姿势直到触地**（`20260923-225916` 帧 2253–2261：
`RootBoneWorldZ − LocationZ` 恒 −101.00、`MeshBoundsZ − LocationZ` 恒 −17.35，而胶囊下落 361 cm）
⇒ 下坠段网格完全冻结 9 帧（高处更久），直到落地表现淡入。

**根因：`UAbilityTask_MHGZPlayMontageAndWait::TickTask` 一直是死代码** —— `bTickingTask` 默认 false，
而登记 tick 的**唯一时刻**是 `Activate()` 返回后那一次 `OnGameplayTaskActivated()`
（`GameplayTasksComponent.cpp:86`，驱动者 `UGameplayTasksComponent::TickComponent`），
之后再置 true 静默无效。⇒ 构造函数里 `bTickingTask = true`（引擎三个 tick 任务同款做法）。

**顺带解释了 22:30 那版为什么无效**：即使开关打开、判据正确，那条路径也永远不会跑 ——
**两个独立原因叠在同一个死代码上**（配置层：默认关；机制层：任务根本不 tick）。

**新增回归钉** `MHGZ.M5.Aerial.AirDodgeVisualTaskTicksAndHoldsTerminalPose`（真实激活一条空回，**四条**）：
① 任务在 `UGameplayTasksComponent` 的 **ticking 列表里**（不是只看旗标 —— 那个位粘性，挡不住「把置真挪到
`ReadyForActivation()` 之后」）；② 实例 `bEnableAutoBlendOut == false` **且资产仍为 true**（实例位从资产拷来，
两半合起来才证明是那行 C++ 关的）；③ 该蒙太奇**单段**（判据前提；不钉「末段末端 == 长度」—— 单段下
`GetSectionStartAndEndTime` 内部就取 `GetPlayLength()`，那是恒真断言）；④ 机制端到端：把位置推到长度 +
`DriveTickForTest` 手动驱动一次 ⇒ `OnCompleted` 真走到 `bAirDodgeVisualFinished`。
**钉子打过变异验证**：注释掉构造函数那行 ⇒ 该测试红 + 防御日志同时响；恢复 ⇒ **M5 = 27/27**。
详见 `空中下落实现缺口.md` §6.5.1。

### 22:50：**清掉空回相关两版修法，按撑杆跳配方重做交棒**（用户指令：「将空回相关清掉,参考撑杆跳重做」）

**先说两件必须说清的事（第二次 PIE 录制的判读）**：

1. **那两轮都是对照轮**。`Saved/Logs/MHGZ.log` 里的 `Cmd:` 只有 `mhgz.Telemetry.Enable 1/0`（+ 遥测自己打的
   `log LogRootMotion Log/Warning`，以及一条误敲的 `AerialFallingRootMotionDisabledByHost=1`）——
   **`mhgz.Aerial.HandoffOneFrameEarlier 1` 从未被执行**，`[AirDodgeHandoff]` 一行日志也没有
   （`Save/Logs` 里 0 次）。所以「问题依旧存在」对 22:30 那版实现**没有构成任何测试**。
2. **两个缺陷都同型复现**（`20260923-214103` / `20260923-214149`，各 4 次空回）：
   缝仍是一帧无蒙太奇（`AirDodge w=1.00 → 0.24 → （无）→ AerialFall_W 0.50 → 1.00`），
   `MeshBoundsZ − LocationZ` 仍 −17.8 → −30.2/−30.5 → −33.5 → 回弹；速度仍在缝上 900 → 0。

**22:50 的真因（比「提前一帧」更根本）**：不是谁先调 `BeginAerialFalling`（两边都在 `EndAbility` 里调、
都有同一行 `Montage_Stop(0.05f)`），而是**收尾那一帧蒙太奇实例还在不在**：

| | 撑杆跳弧段 `*_Over` | 空回 `AirDodge`（旧） |
|---|---|---|
| `bEnableAutoBlendOut` | **false**（`PlayVaultMontage` 显式设；注释「两种情形都不要改成 true」） | true（旧注释还专门解释「特意不动」） |
| 到自身长度 | 不终止，位置夹在 `长度−5e-5`、权重保持 1.0 等在那里 | 自动淡出 → `Terminate()` → `OnCompleted` |
| 收尾触发点 | 自己的移动源（`HandleBackVaultMovementFinished`） | 这条蒙太奇的自然播完回调 |
| `EndAbility` 那帧 | 实例**还活着** ⇒ `Montage_Stop(0.05)` 与下落蒙太奇**同帧交叉** | 实例**已经没了** ⇒ 一帧无蒙太奇 |

实测：撑杆跳 `20260923-194246` 帧 2882–2885 为 `1.00 → 0.50+下落 0.50 → 0.00+1.00 → 1.00`，弧段位置冻在
`1.1029`（= 该蒙太奇长度）保持；空回则是 `1.00 → 0.24 → （无）→ 0.50 → 1.00`。

**删除清单（「空回相关」）**：

| 删掉 | 位置 |
|---|---|
| `RequestEarlyCompletion()` / `ShouldCompleteEarly(...)` / `TickTask` 提前广播 / 两个旗标 / 日志计数 | `AbilityTask_MHGZPlayMontageAndWait.{h,cpp}` |
| 开关 `mhgz.Aerial.HandoffOneFrameEarlier` | 同上 |
| `StartAttackMontage` 里的 `RequestEarlyCompletion()` 调用 | `MHGZAirDodgeAbility.cpp` |
| 窄门控 `mhgz.Aerial.NeutralizeNegligibleAnimRoot` / `mhgz.Aerial.NeutralizeEpsilonCm` / `IsNegligibleAnimRootNeutralizationEnabled` / `GetNegligibleAnimRootEpsilonCm` / `NeutralizeNegligibleAnimRootMotion` / 计数器与日志上限（含 `TickCharacterPose` 两处调用） | `MHGZInstrumentedCharacterMovementComponent.{h,cpp}` |
| 回归钉 `MHGZ.M5.Aerial.HandoffOneFrameEarlierWindow` | `MHGZM5AirDodgeTests.cpp` |

⚠ 窄门控被删后**当前不存在任何与空回相关的 CVar**（要动写点只能改代码）。缝上抹零的**症状**已在 23:00 的配方后消失（`20260923-235744`：缝上 7/7 恒 900、下坠段保持 900），但**写点未修** —— 机制在别处仍触发（同录制 `AM_IG_TuCi` 一帧 424 → 0）。**不要**把「症状已消」读成「P0-1 已修」。

**重做（撑杆跳配方，两处，都在空回侧）**：

| 改动 | 文件 |
|---|---|
| `MontageInstance->bEnableAutoBlendOut = false` —— 与 `PlayVaultMontage` 同一行、同一个理由 | `MHGZAirDodgeAbility.cpp` |
| `EndAbility` 的 `Montage_Stop(0.05f, ActiveAttackMontage)` **去掉 `if (!bAirDodgeVisualFinished)` 守卫**（撑杆跳那条守卫在本招是错的：本招「完成」时实例还活着，正是要淡出的对象） | 同上 |
| 任务新入口 `ReportCompletionAtMontageEnd()` + 纯判据 `HasMontageReachedEnd(Position, PlayLength)`（容差 1e-3 s，> 引擎末尾夹取 `长度−5e-5`、< 一帧两个数量级） | `AbilityTask_MHGZPlayMontageAndWait.{h,cpp}` |
| 回归钉改名并重写为「到达长度才报、还没到不报、退化输入不卡死」：`MHGZ.M5.Aerial.MontageEndCompletionWindow` | `MHGZM5AirDodgeTests.cpp` |

**为什么不照抄「让空回的移动源收尾」**：本招位移源 ≈0.995 s 就结束，而真值里 `137` 是 142 帧、
最后 0.188 s 的姿势属于本招 ⇒ 提前收会剪掉尾段（`.h` 结构性事实 3）。

**验收判据（下次 PIE，默认行为即新配方，**不需要开任何开关**）**：
① 缝上出现「空回 `w>0` 与下落 `w>0` 并存」的一帧（`MontageInstances.csv`）；
② `MeshBoundsMinZ` 与 `MeshBoundsZ − LocationZ` 不再出现 ~15 cm 下陷/弹回，改为一支平滑曲线；
③ 胶囊弹道七项不变；`AerialMovementDiscontinuity` 在缝上**不再出现**（实测 7/7 恒 900、日志 0 条）—— 但写点未修，该告警在其它动作仍会命中（同录制 `AM_IG_TuCi` 一帧 424 → 0），**另立一条判据、不并入缝上验收**；
④ 用户观感为最终判据。

### 22:30：按用户提议「参照撑杆跳的下坠」实现交棒提前一帧（**22:50 已按用户要求清掉**）

> 保留本节只为留痕：它记录的是「提前量」思路，22:50 的撑杆跳配方取代了它（两者的差别见缺口文档 §6.5）。
> 关键教训：这一版从未被测过 —— 两次 PIE 录制都是对照轮（见上），**「没有效果」的判定当时并不成立**。

### 21:50：用户观感否掉「顿挫=速度骤变」—— 顿挫定位到**姿势层的交棒空档**

用户对同一批 A/B 录制的判定：**顿挫仍在，开关几乎无区别**。⇒ 遥测侧那个 `900→0` 骤变确实被窄门控修掉了，
**但它不是用户看到的那一下**。按审查文档自己的规矩（「若胶囊 Z 已连续而画面仍像竖直停顿，单独比较淡出尾帧、
无蒙太奇帧与下落入场帧的 pelvis/脚和镜头位置」）转查姿态层，读数如下（录制 `20260923-211851`，帧 2417 缝）：

```
F=2416  AM_IG_AirDodge      w=1.0000  pos=1.1503
F=2417  AM_IG_AirDodge      w=0.2411  pos=1.1754      ← 淡出
F=2418  （无蒙太奇实例）     GA 仍活跃（ActiveActions=GA_IG_AirDodge_C#6）   ← 空档帧：姿势由底层图（MM）驱动
F=2419  AM_IG_AerialFall_W  w=0.5017  pos=0.0251      ← 下落入场（半权重，还需两帧才到 1.0）
F=2420  AM_IG_AerialFall_W  w=1.0000
```

| 量（缝 ±5 帧） | 读数 |
|---|---|
| 胶囊 `LocationZ` / `VelocityZ` | 平滑弹道（vz 匀加速、逐帧 ΔZ 与重力吻合）⇒ **胶囊无顿挫** |
| `MeshRelativeToCapsuleZ` / `RootBoneRelZ` | 恒定（10.00 / −111.00） |
| **`MeshBoundsZ − LocationZ`（姿态相对胶囊的高度）** | 缝前稳定 **−17.5**，缝后稳定 **−17.5**，**中间塌到 −30.5 → −33.4 → −28.1（3 帧，深 ~15 cm）再弹回** |
| 同一量的水平/朝向 | `MeshBoundsX` 逐帧平滑、`ActorYaw` 恒定 ⇒ 只有竖直向的姿态塌陷 |
| 对照组（开关关）同帧 | **完全同型**（峰值偏离 15.3~17.5 cm）⇒ 与本轮改动无关，是既存缺陷 |

**判读**：缝两侧各自的稳定姿态一致（≈−17.5），中间却「瘪下去再弹回」= **过冲**，不是换到新姿态；
再叠加「一帧无蒙太奇 + 两帧半权重混入」，构成 ~50–75 ms 的姿势空档 ⇒ 与用户观感一致。
⇒ **顿挫的主因在姿势交棒，不在速度**；按文档 §E 的边界，**不要用改弹道/改速度去治它**。

**下一步候选（2026-09-23 24:00 回填状态）**：
1. **消掉空档帧**：让下落蒙太奇在空回蒙太奇仍有权重时就开始（提前一帧启动 handoff，或用
   `BlendOutTriggerTime`/notify 触发），使交接成为真正的交叉淡入而非「先死一帧再入场」。
2. ✅ **已实施（22:50，即「撑杆跳配方」）**：**让空回蒙太奇不要自己先死** —— `bEnableAutoBlendOut=false` + 显式带混合地停止（注意
   `PushDisableRootMotion` 的历史坑，见 `docs/using/m5-plan.md` 附录 A）。
3. **空档帧别掉回地面姿势**：空中给 MM 一个「保持上一个空中姿势」的路由。
4. 补探针把「可见」量测出来：pelvis / 双脚 / 镜头位置（当前只有 `MeshBounds*` 这个间接量）。

### 21:35：窄门控 A/B 验收 —— **缝 6 → 0，且无附带损伤**（⚠ **该实现与两个 CVar 已于 22:50 按用户要求整体删除，本节仅留痕**）

用户同一次 PIE 内做了两轮：`20260923-211754`（开关关，对照）与 `20260923-211851`（帧 954 处
`mhgz.Aerial.NeutralizeNegligibleAnimRoot 1`，实验）。

| 读数 | 对照（关） | 实验（开） |
|---|---|---|
| `AerialMovementDiscontinuity`（`900→0.1~0.3` 断点） | **6 次**（帧 418/544/894/1078/1279/1627） | **0 次** |
| 空中样本中 `HasAnimRootMotion=1` | **220** | **0** |
| `ConstrainAnimRootMotionVelocity` 投票（空中） | 2188（**40**） | 1374（**0**） |
| `RootMotionShieldBound=1` 的样本 | 0 | 0（⇒ 修复**不依赖**那个会失效的 Host 委托） |
| 空回 6 次跑满的七项标量 | 全 OK（v0z 1179.3 / g −2369.0 / 净 1066.5,−265.8 / 顶点 293.5 / R² 0.99998） | **全 OK 且逐值同量级**（1179.1 / −2369.2 / 1066.5,−266.2 / 293.4 / 0.99998） |
| 落地 `LandingSpeedReset` 写后 v2D | 6/6 = **337.0** | 6/6 = **337.0** |

判读：实验组里**空中再没有任何一帧动画根标志为真** ⇒ 动画根分支整段不执行、`Constrain` 空中 0 次
⇒ XY 不可能被替换；同时**地面合法根运动照走**（地面 Constrain 投票仍在 1374 次，`|d|` 全在 1–7 cm 的合法带，
ε=0.05 从未命中）⇒ 上一版「整体作废」的副作用这次不存在。轨迹与落地值不变 ⇒ 公共代码未被破坏。

**本轮顺带修掉一个探针缺陷**：gate 的 60 行日志上限被**地面 idle 帧**（`vZ=0`、`vXY≈0`）先吃光，
真到缝上反而看不到行。已改为**只叙述「空中 或 `vXY>100`」的命中**（命中计数照记，含原文的显式截断提示）
——此后缝上必有行。（⚠ 随窄门控删除，`[AerialAnimRootGate]` 已不存在；缝上的复测判据改用 `MovementPhases/Spatial` CSV 的 vXY 与蒙太奇交叠帧。）当时的日志判据是：`[AerialAnimRootGate]` 行应出现在空回淡出帧附近、
`falling=1`、`|d|` 在 1e-3 量级。

> P1-1 备注：本轮 12 次真实落地的**入速**是 118~313 cm/s（不是 ~900），仍是「约 300 cm/s 档」
> 一类样本；所以「300 / 900 两档同相位验收」还缺一档——需要一个带 ~900 入速的落地试次。

### 21:10：步骤 1 完成（来源定名）+ 步骤 2 窄门控已实现（⚠ **该窄门控与两个 CVar 已于 22:50 整体删除，本节仅留痕；当前空回行为 = 默认行为，无任何开关**）

**PIE 取数（录制 `20260923-210402-…-55482`，9 次断点、321 行 `[AerialRootMotionFeed]`、未触顶）**：

断点帧长这样（448 / 637 / 994 / 1562 … 共 9 处，形态一致）：

```
Frame=448 dt=0.0251 animRM 0->1 RMPtrans=(0.0545,-0.0219,0.2731)->(-0.0000,-0.0016,-0.0000)
   RMProt=(0,0,0)  vXY=900.0 vZ=-1509.0  playRM=1(fromEverything=1,networkedMontage=0)
   RMImontage=None  montages=1  actions=[GA_IG_AirDodge_C#5]
```

**幅值统计（321 行的实测分母，正是文档要求的那份「合法 vs 残余」）**：

| 区间（cm/帧） | 全部行 | 空中行（`vZ<-100`，共 29 行） |
|---|---:|---:|
| <0.01 | 54 | **28** |
| 0.01–0.1 | 4 | 1 |
| 0.1–1 | 3 | 0 |
| 1–5 | 59 | 0 |
| ≥5 | 201 | 0 |

⇒ **合法根运动（1–7 cm/帧）全部出现在 `vZ=0` 的地面帧**（撑杆跳起手段等，且带真实蒙太奇实例）；
**空中的动画根位移恒 ≤0.0072 cm/帧（≈0.3 cm/s）**，与 CMC 自己的 900 cm/s 差 3 个数量级。
⇒ 文档「不能只凭幅值划分」的顾虑**已被数据回答**：本项目里两组之间空着两个数量级，没有交叠。

**来源定名（步骤 1 的答案）**：写入者是
`Runtime/Engine/Private/Animation/AnimSync.cpp:98/102` —— `InProxy.ExtractedRootMotion.AccumulateWithBlend(..., TickRecord.GetRootMotionWeight())`：
**同步组里每个资产播放器按自己的根运动权重**往代理累加。配合 `AnimationAsset.h:800`
（首次累加即 `Set(InTransform * VBlendWeight)`，幅度先乘权重）与 `AnimInstance.cpp:755` 的 `MakeUpToFullWeight()`
（再向单位变换插值一次）⇒ 权重不满时幅度掉到 **≈w² 量级**，于是「标志为真、位移近零」。
这也解释了地面/空中的分野：地面 locomotion 播放器权重≈1（真位移），空中权重被蒙太奇切碎（≈噪声）。
（顺带：`GetRootMotionMontageInstance()` 在 `RootMotionMode=RootMotionFromEverything` 下恒 null，
所以它**不能**用来排除蒙太奇轨；排除蒙太奇轨的真凭据是 `Montage->HasRootMotion()=0`。）

**步骤 2 的实现（本轮）**：不改任何「整体作废」的门，只加一条**幅值窄门**，落在**没有绑定漏洞的那一层**：
`UMHGZInstrumentedCharacterMovementComponent::TickCharacterPose`（项目 CMC 子类）在 `Super::TickCharacterPose`
之后调用 `NeutralizeNegligibleAnimRootMotion()` —— 若 `RootMotionParams.bHasRootMotion` 且
`|Δroot| < ε`，就 **`RootMotionParams.Clear()`**，于是 `HasAnimRootMotion()` 为假、**动画根分支整段不执行**
（既不替换 XY、也不碰旋转，也不依赖 Host 那个换武器后会失效的委托绑定）。

> ⚠ **下面这张表里的两个开关已随 22:50 的删除清单一并移除，今天在 PIE 里设不了**（仓库 grep 零命中）。

| 开关（**已删**） | 默认 | 说明 |
|---|---|---|
| `mhgz.Aerial.NeutralizeNegligibleAnimRoot` | **0（关）** | 上一版「整体作废」被 PIE 否决过，故窄门默认关，PIE 内可直接 A/B，无需重编 |
| `mhgz.Aerial.NeutralizeEpsilonCm` | 0.05 | 噪声 ≤0.0072、合法 ≥1.0 ⇒ 两侧各留约一个数量级余量 |

触发时打印 `[AerialAnimRootGate]`（前 60 条 + 显式截断提示）。**当时的待验项（已随实现删除，不再适用）**：`mhgz.Aerial.NeutralizeNegligibleAnimRoot 1`
下缝上 vXY 不塌且**其它空中动作观感不变**（因为只对 <0.05 cm 的根位移生效，合法根运动原样放行）。

> 另：`LogRootMotion` 开关上一版没生效（5.6 里 `FindConsoleVariable("LogRootMotion")` 返回 null），已改为走
> `GEngine->Exec(nullptr, "log LogRootMotion Log")`；本条来源既然已定名，这条叙述只作**旁证工具**保留。

### 20:40：步骤 1 —— 动画来源探针就位

**引擎侧先把两条候选通道读清了（源码，非推断）**：

| 通道 | 闸门 | 结论 |
|---|---|---|
| 蒙太奇根运动（`QueueRootMotionBlend`） | `AnimMontage.cpp:2486` `bExtractRootMotion = (OutRootMotionParams != nullptr) && Montage->HasRootMotion()`；`2580` 再查 `!IsRootMotionDisabled()`；混合权重 = `Blend.GetBlendedValue()` | **本项目此路不通**：表现蒙太奇的 `HasRootMotion()` 已是 0（clip 剥离后）⇒ 理论上不供能 |
| 动画图（`Proxy.GetExtractedRootMotion()`） | `AnimInstance.cpp:735-741` 把它并进 `ExtractedRootMotion`；`744-748` 混入蒙太奇队列；`753-755` `MakeUpToFullWeight()` 把部分权重补到 1 | 全引擎**没有任何 writer** 写代理的 `ExtractedRootMotion`（`grep GetExtractedRootMotion()` 只命中这一处读取 + accessor）⇒ 图侧理论上也不供能 |
| URO 插值 | `ConsumeRootMotion_Internal(Alpha)` → `ConsumeExtractedRootMotion(Alpha)` | 仅当 URO 生效且 Alpha<1 才走插值；项目配置未见 URO 开关 |

**两条理论通道都不通、`RootMotionParams` 却翻真** ⇒ 必须抓现行，故加两个探针（本轮已编译、M5 25/25）：

1. **CMC 侧 `TickCharacterPose` override**（该函数是 virtual，`CharacterMovementComponent.h:2401`）——它正是
   `PerformMovement:2824` 里把动画喂进 `RootMotionParams` 的那一步。每条日志
   `[AerialRootMotionFeed]` 记：`animRM 前->后`、`RootMotionParams` 位移**与旋转**（前后）、
   `playRM`（含 `fromEverything`/`networkedMontage` 两个判据）、`shouldTickPose`、蒙太奇实例数、
   **根运动蒙太奇实例的名/位置/权重/`rmDisabled`**、以及当时的 Host 运动所有者与活跃动作串。
   只在「本帧确有根运动（前或后为真）」时打印。
2. **打开引擎自带的根运动叙述**：`LogRootMotion` 声明级别是 **Warning**
   （`EngineLogs.h:17`），所以 `FAnimMontageInstance::Advance ExtractedRootMotion`（`AnimMontage.cpp:2587`）
   与 `PerformMovement WorldSpaceRootMotion`（`CharacterMovementComponent.cpp:2877`）**平时全被静音**——
   这也是此前日志里一条都搜不到的原因。现在**随遥测开/关自动切到 `Log`/回到 `Warning`**
   （`MHGZMotionMatchingAnimInstance` 的 `SetRootMotionLogVerbosity`）。这两行一旦出现，
   就直接指名「谁供给了这一帧的根运动」（前者=蒙太奇轨、后者=CMC 帧内应用），并带数值。

**判读口径（下次 PIE 后）**：断点帧附近应出现以下三种之一 ——
① `FAnimMontageInstance::Advance ExtractedRootMotion: (...) bBlendRootMotion: 1` ⇒ 蒙太奇轨（则查 `HasRootMotion` 为何与遥测不符）；
② 只有 `PerformMovement WorldSpaceRootMotion Translation: (...)` 且 `[AerialRootMotionFeed]` 里 `RMImontage=None` ⇒ 图侧供给（则继续追 AnimGraph 的根运动节点）；
③ 两者都没有、`animRM` 仍翻真 ⇒ 落点在我尚未覆盖的第三条路径（下一步查 `RootMotionParams` 的全部 writer）。
同时从这些行里量**合法**空中根位移/旋转（撑杆跳、舞踏、空中攻击、空回尾段、落地表现），
为步骤 2 的门控提供分母；旋转单独记，不由位移幅值决定。

> 备注（含一条可复用的操作事实）：`mhgz.Telemetry.Enable 1` **不要在编辑器启动阶段/世界存在之前设置**。
> 实测三次把 `mhgz.Telemetry.Enable 1` 放进 `-ExecCmds` 的自动化跑法，都不进入自动化：
> 日志里 `Cmd: mhgz.Telemetry.Enable 1` 已在 frame 0 执行且 CVar 为 1，随后编辑器以约 2 FPS 空转数分钟、
> `LogAutomationController` 一行不出（对照跑——同一构建、遥测关——`EXIT=0`、`MHGZ.M5` 25/25、约 2 分钟完事）。
> 结论：**遥测的既定用法是「进 PIE 之后再开」**（本项目此前的所有录制都是这么做的），
> headless 冒烟用不了这条路；探针路径的实测验证只能靠 PIE。

### 20:00：回退版重现，写速函数确认，修法仍未定

最新会话 `20260923-200039-…-55482` 保留 P1-3 子阶段探针，撤回 19:42 的根盾重绑行为。13 次无阻挡空回都在淡出帧由 `ConstrainAnimRootMotionVelocity` 将 XY `900 → 0～0.391 cm/s`；同一帧垂直速度继续按重力变化。`RootMotionShieldBound` 在 34334 条 CMC 样本中全为 0，日志只有一次 `bound` 和紧随的一次 `unbound`，与当前 `RebuildRuntime` 未重绑一致。空回蒙太奇的 `MontageHasRootMotion=0`，但 CMC 帧内 `HasAnimRootMotion=1`；AnimInstance 的 `AnimRootMotionMode=2` 即 UE 5.6 的 `RootMotionFromEverything`。**写速函数已定名，贡献近零根运动的动画图节点/序列还没有定名。**

19:42 的重绑尝试曾使该类归零不再出现，却同时使其它动作观感回归；它不能作为可接受修法。下文“近零根位移阈值”也是**待验证候选**，不是下一版必做配置：合法空中动作可能短暂停在近零根速度，单看幅值不足以区分无效残余与有意停顿。先追根运动来源，再按动作所有权/阶段决定是否需要更窄的门控，并对根旋转独立验收。

### 19:55：根盾这版**已回退** —— 用户 PIE 判定「没修好，还把原来好的弄坏了」

**用户 PIE 结论（2026-09-23 19:42 录制 `20260923-194246-…-55480`）：症状未消，且出现新回归。**
遥测侧看到的与观感不一致，两边都要记：

| 观测 | 读数 |
|---|---|
| `900 → 0.3` 那个缝 | **本次会话一次都没出现**（子阶段记录全程有；`shieldBound=1` 14189 样本） |
| 唯一的空中塌缩 | 帧 2584 `900 → 53.9`，**`BlockingImpact=1`、`Actor=StaticMeshActor_101`、法线沿 Y** ⇒ 真实撞墙，不是本缺陷 |
| 其余塌缩 | 均为 `vZ=0` 的落地帧（`LandingSpeedReset` 一类的正常重设） |
| 根盾触发 | `bound` 2 / `unbound` 2 / **`shielding anim root` ≥20（日志上限）** —— 盾这次真的通电了 |
| `ConstrainAnimRootMotionVelocity` 调用 | 1939 次，其中绝大多数是**地面 locomotion**（`in≈(-0.39,-0.11)`，盾按设计放行） |

**⇒ 盾在机械上止住了缝，但作用面过宽，判为不可接受**：它挂在
`ConvertLocalRootMotionToWorld`，只要「非地面或托管表现窗口」就把**动画根的位移换成 `Velocity*Δt`、旋转换成单位**。
日志里能看到它**丢掉了真实的根位移**（例：`in=V(X=6.06, Y=-3.55, Z=0) out=V(X=5.43, Y=-7.03) v=V(X=215.05, Y=-278.16)`，
角色以 ~351 cm/s 在空中运动时每帧都在替换）。**具体坏掉的动作用户未逐条点名**，
候选面（未验证）：撑杆跳/空中攻击等**合法使用动画根位移或根旋转**的空中动作，其 mesh 位移与朝向被改成
CMC 速度/单位旋转；落地表现窗口同理。⇒ **不能靠「把空中动画根一律作废」来修这一条缝。**

**已回退**（编译 Succeeded）：

| 项 | 状态 |
|---|---|
| `RebuildRuntime` 里的 `BindAerialRootMotionShield()` | **已删除**（→ 盾回到「换武器后失效」的原状态，即用户观测到的「原来好的」那版） |
| `MHGZ.M2.Equipment.SameWeaponArmorSocketAreNoOp` 里新增的绑定断言 | **已删除** |
| `Unbind` 的 `[AerialRootMotionShield] unbound from CMC …` 日志 | **保留**（纯诊断、零行为影响；要一并撤请说一声） |
| P1-3 子阶段记录（`MHGZInstrumentedCharacterMovementComponent` 等） | **保留**（判因工具，遥测关时等价透传） |

**曾提出的下一版候选（未实施，20:00 复核后不能直接照做）—— 窄化到「近零根位移才替换」，并保留旋转与合法根位移**：
断点帧的动画根位移是 `(-0.0077, 0.0059, -0.0000)`（≈0.3 cm/s），而同期**合法**的空中根位移在
50~240 cm/s 量级（上表日志）。这批样本的幅值虽有区分度，**不能据此断言全动作可由 epsilon 划分**。原候选设想在验证合法静止/转向帧后，于特定空回尾段按
`|Δroot|/Δt < ε`（曾拟 ε≈10 cm/s）决定是否把根位移换成 `Velocity*Δt`（使 `Constrain` 对 XY 变恒等），
否则**原样放行**；根旋转需要独立的所有权规则，不能由位移幅值决定。并且门要用「空回尾段」这类**窄条件**，
不要用「只要在空中」。⚠ 注意：**直接返回单位变换是错的** —— 那会让 `Constrain` 拿到 `0/Δt = 0`，
等于亲手把 XY 抹掉（这正是本缺陷的原始机制）。

### 19:35：写手定名 —— `PerformMovement:2870` 的动画根替换，**根盾从未通电**

P1-3 仪表一次 PIE 就定案（录制 `20260923-193250-…-55480`，断点帧 820，6 次空回全复现）：

```
CMC.HandlePendingLaunch.Post        v=(716.3779, -544.8013, -1507.9955)  HasAnimRootMotion=0
CMC.ConstrainAnimRootMotionVelocity callSite=None ⇒ 来自 PerformMovement:2870
    RootMotionParamsTranslation=(-0.0077, 0.0059, -0.0000)
    AnimRootMotionVelocity=(-0.3026, 0.2301, -0.0000)
    ConstrainInput=(716.3779, …)  →  ConstrainOutput=(-0.3026, …)
CMC.StartNewPhysics.Pre             v=(-0.3026, 0.2301, -1507.9955)   ← XY 被换、Z 保留
```

即 `A2` 表第二行（「进入 CMC 时 900、出来 0，且动画根分支真的执行」）——**动画根行是写手**，
`AnimRootMotionVelocity = RMPtrans/Δt` 与断点后速度逐位吻合。

**但根盾当时是死的**：真实遥测里 `RootMotionShieldBound` **每一个样本都是 0**。原因是绑定生命周期：
`InitializePawnRuntime` 绑定 → 首次武器身份变化走 `TeardownRuntime`（`Unbind`）→ `RebuildRuntime`
（**漏了重绑**）⇒ 整局失效，日志因此只有一条 `bound`、0 条 `shielding anim root`。
（我 18:40 那版「动画根被证伪」的结论据此作废：它把「回调没被调」当成「分支没走」，
而前提——回调在岗——本身不成立。）

**当时的修复（已于 19:55 回退，见上一节）**：`RebuildRuntime` 末尾补 `BindAerialRootMotionShield()`（幂等）+
`Unbind` 补日志 + M2 回归钉。它让盾真正通电（`shieldBound=1`、`shielding anim root` ≥20），实测**止住了缝**
（19:42 会话无 `900→0.3`），但**作用面过宽、引入观感回归**，用户判定不可接受 ⇒ 两处代码改动已删除，
只保留 `Unbind` 诊断日志与 P1-3 记录层。教训：**「把某个来源整体作废」是能止症的最省事解法，也是最容易打坏别处动作的解法** ——
改前先量它替掉的是不是全是噪声（本例：噪声 ~0.3 cm/s vs 合法 50~240 cm/s，可分，本可窄化）。

### 18:30-18:40：P1-3 落地 + 三个候选被既有录制否掉

**A 节步骤 1 已实施**（`MHGZInstrumentedCharacterMovementComponent`，编译 Succeeded、`MHGZ.M5` 25/25、退出码 0，遥测关时逐字节等价透传）：

| 新增记录点 | 用途 |
|---|---|
| `RestorePreAdditiveRootMotionVelocity` / `CalcVelocity`（带实际 `Friction`/`BrakingDeceleration`）/ `ApplyRootMotionToVelocity` / `PhysFalling` / `ApplyAccumulatedForces` / `HandlePendingLaunch` / `UpdateCharacterStateBeforeMovement` / `StartNewPhysics` 的 Pre+Post | 把「一个 `PerformMovement` 内部」切成具名区间，相邻两条 vXY 一变即指名函数 |
| `ConstrainAnimRootMotionVelocity`（const override + 每次投票一行） | 直接证明动画根分支**是否被进过**；`ConstrainCallSite` 标出是哪一层调的 |
| 每条样本新增 31 列：`SubPhaseSite`/`SubPhaseEntryVelocity*`/`HasAnimRootMotion`（= `RootMotionParams.bHasRootMotion`，唯一合法采样时点）/`AnimRootMotionVelocity*`/`IsPlayingRootMotion`/`RootMotionShieldBound`/`AdditiveVelocityApplied`/`LastPreAdditiveVelocity*`/`Constrain*` | 判读哪一类写手 |
| `MHGZ.log` 的 `[AerialMovementDiscontinuity]` 告警追加**本次调用内全部子阶段逐行 dump** | 不等 CSV 就能就地读出错帧的写手 |
| 采样门 = 遥测开 且（`IsFalling()` 或 `IsFlying()` 或 Host 托管表现窗口） | 覆盖前一段飞行与整段下坠，不做全程逐帧大日志 |

**用既有录制+日志先否掉的候选**（无需新 PIE）：

1. ~~**动画根 / `ConstrainAnimRootMotionVelocity` —— 否**~~ **← 这条已作废（19:35 定名为此写手；当时依据的「盾 0 回调」因盾未绑定而无效；`ProxyHasRootMotion=0` 是我自己记录过的采样陷阱）。保留以示教训：排除项必须写清它依赖的前提。**
2. **制动 / `CalcVelocity` —— 否。** 断点帧 `Acceleration=(0,0,0)`、`BrakingDecelerationFalling=0`、`FallingLateralFriction=0` ⇒ `bZeroAcceleration && bZeroBraking` ⇒ `CalcVelocity` 不写 Velocity。
3. **源覆盖 / 源结束 finish-velocity —— 否。** `ActiveRootMotionSourceCount=0`、`HasOverrideRootMotionVelocity=0`；全会话 `FinishVelocityMode=0`（MaintainVelocity，`SetVelocity` 出现 0 次）；`JumpForce` 源在断点前 6-7 帧退出，退出后 6 帧 XY 仍 900。

⇒ 当时把剩余候选夹在 `PerformMovement` 的 `[Pre → ApplyAccumulatedForces.Pre]` 与 `PhysFalling` 的扫掠/回推分支之间（第 1 条作废后的结论，见上一节 19:35 定名）。

**判读口径（复用）**：断点帧里第一条 vXY 塌掉的样本 → 它的 `Phase`/`SubPhaseSite` 就是写手所在函数；`ConstrainProbe=1` 且 `callSite=None` ⇒ 来自 `PerformMovement:2870`。三个候选（制动、源覆盖、源结束）的排除依据与「哪些前提必须在场」见下一节与附录 A.1 第 28 条。

### 17:53 PIE 后的状态（**历史快照**；`P0-1/P0-2/P1-1/P1-3` 的当前状态见文首 24:00 审计表）

- **P0-1 未修：** 14/14 次完整空回在蒙太奇 `1.175 s` 淡出帧内由 `Super::PerformMovement` 将水平速度 `900 → 0.04～0.38 cm/s`，无 `HandleImpact`，无有效 RootMotionSource；GA 完成和 Host 下落接管均在下一帧。垂直速度/位移保持正常重力曲线。上一轮碰撞归因已撤回，现需锁定 CMC 内首次写速子阶段。
- **P0-2 未达到目标：** 现有自动化在测试角色上手动触发视觉完成，未让生产 BP/AnimInstance 经过实际出错的淡出帧；其通过与 PIE 的 14/14 失败并不冲突。新增回归须真实推进 `BP_IG_Character` 的动画和 CMC。
- **P1-3 部分完成：** 最新 CSV 已证明 14 次断点均发生在 CMC 更新内，精确命中列在真实撞墙时有效，异常告警也产生；但仍缺 `CalcVelocity`、根运动恢复/约束等 CMC 子阶段数据，尚不能给出具体写速语句。
- **P1-1 部分验收：** 14 次真实触地的一次性帧末校正均让本帧 CMC 后速度为 `337 cm/s`；因空回先丢速，本批触地入速仅 `0～0.2 cm/s`，仍需约 300/900 cm/s 两档验收。**（24:00 回填：两档数据已于 `20260923-225916`/`235744` 取得，写后均 337.0；剩余的是采样相位判据 —— 见 §D。）**
- **P1-2 暂缓：** 落地动作结束后的 MM 姿态抽搐按用户要求留待之后处理。

## 修复目标与边界

| 级别 | 问题 | 完成定义 | 状态（2026-09-23 24:00 逐条审计，HEAD `e0b105e`） | 依据 |
|---|---|---|---|---|
| P0-1 | 空回 `137 → 157` 交接时 XY 速度约 `900 → 0 cm/s` | 确定**帧内实际改写点**，在该点修正；完整空回离地、无碰撞时速度和胶囊位移连续，竖直弹道保持连续。不得只在 `157` 起播时补写 `900`。 | **未满足（症状已消、机制未修）** —— 完成定义 (a) 满足、(b) 未满足 | `20260923-235744`：缝上 `vXY` 7/7 恒 900、下坠段保持 900（7 帧走 157.3 cm）；写点 `PerformMovement:2870` 的 `ConstrainAnimRootMotionVelocity` **未动**、窄门控已删；同录制 `AM_IG_TuCi` 一帧 424 → 0 |
| P0-2 | 自动化只验盾和资产、不验实际交接 | 使用真实角色、能力、蒙太奇和 CMC 的逐帧回归能在旧版本失败、修复版通过；资产静态测试继续保留但不代替它。 | **未满足** | 那条测试的交接由测试接缝在第 26 帧摆出（`SetAirDodgeExitFlagsForTest` + `HandleAirDodgeVisualCompletedForTest`）；测试世界不 tick 且 `AnimInstance` 是基础 `UAnimInstance`；表现资产根运动抽取在段级已关 ⇒ `bHasRootMotion` 恒假 ⇒ 引擎 `CharacterMovementComponent.cpp:2870` 不可达 ⇒ 缺陷有无时都通过。全仓库无一条会在 P0-1 存在时变红 |
| P1-1 | 落地 `337 cm/s` 重设只有函数内测试，没有真实帧时序结论 | 区分 `Landed` 回调写值、同帧 CMC 后值和下一帧值；与 Rise 的 `148` 首帧采用**相同采样相位**比较，两档入速都落在 `337±6 cm/s`。 | **部分满足** | 写点齐、两档入速都有真触地数据（约 900 / 约 300 → 写后均 337.0）；缺真 `LandedDelegate` 的逐帧断言、「下一帧值」零覆盖、「与 Rise `148` 首帧相同采样相位」未落成判据（实测**首采样帧 900、次帧 337**；撑杆跳档首帧即 337） |
| P1-2 | 落地表现结束到 MM 移动存在 Idle / `Walk_Start@0` 断链 | 持续推杆的落地直接进入步态及左右脚相位匹配的 Move loop；无 2～3 帧 Idle、无误选 `Walk_Start@0`、无可见抽搐；放杆仍正确进入 Idle。 | **未实现（用户决定推迟）** | 无 `EMHGZMotionMatchingHandoffType::LandingExit`、`HandleAerialLandingMontageEnded` 不发布 MM 载荷、AnimInstance 无落地专用路由 ⇒ 断链仍是「落地 → Idle 2~3 帧 → `Walk_Start@0`」 |
| P1-3 | 空中运动所有权缺少帧内可追溯交棒 | 每次 RMS、CMC 自由落体、表现蒙太奇、落地运动的交接都能按会话 ID 查询旧/新所有者、触发点、速度及位置；异常有告警而非静默恢复。 | **已满足（已交付）** | 子阶段记录层 + 遥测列；本轮两次定名（task 的 tick 门、缝上的钳制帧）都靠它。残留子项「AnimGraph 近零根位移贡献节点未定名」单列，不属本条定义 |

范围限定于这五项。四向撑杆跳的独立曲线误差、舞踏、空中攻击和其他 P2 文档清理不混进本轮修复；但它们须参加回归，防止共用代码被破坏。

状态取自 2026-09-23 24:00 逐条审计（HEAD `e0b105e`）；当前通过基线 `20260923-235744-BP_IG_Character_C_0-55482`。
**「未修」现在只对 P0-1 的机制与 P0-2 成立，不要在整表上套用一个标签。**

## 实施顺序与提交边界

20:00 回退版既排除这 13 个断点的 `HandleImpact` 碰撞和即时 RMS 到期，也定名 CMC 的动画根约束写速函数。下一步按以下顺序执行：

1. **追 P1-3 尚缺的动画来源。** CMC 子阶段探针已完成本故障判因；从 AnimGraph 的贡献节点/序列追踪近零根运动如何进入 CMC 的 `RootMotionParams`。同时量取撑杆跳、舞踏、空中攻击、空回尾段和落地表现的合法根位移/旋转，覆盖静止帧与转向帧。记录必须能区分哪一个动作拥有胶囊位移，不能用蒙太奇 `HasRootMotion=0` 代替帧内证据。
2. **设计并修 P0-1 的窄门控。** 在已确认的 `ConstrainAnimRootMotionVelocity` 写速链上，按空回尾段运动所有权处理非预期根速度。先定义作用窗口与退出时机，保留其它动作合法根位移和旋转，再决定是否需要幅值阈值；不得一律屏蔽空中动画根，也不在下落入口补写 900。
3. **补 P0-2 的真实淡出回归。** 让 `BP_IG_Character` 与其 AnimInstance 正常 tick 到空回蒙太奇淡出、实例消失和系统下落起播，不手动调用视觉完成回调；逐帧断言 XY 速度/位移连续、Z 与重力相符、无阻挡命中。旧版本必须在约 `1.175 s` 失败，修后通过，再跑相关 M5 与整组自动化及实际 PIE。
4. **单独验 P1-1。** 修好空回 XY 后可得到约 900 cm/s 的自然落地入速；再安排约 300 cm/s 的撑杆跳/其它下落入速，核对两档的 `Host.PlayLanding` 写前后、`CMC.LandingSpeedReset.Applied`、同帧 CMC 后和下一帧。近零入速 14/14 的 337 结果已记录，不重复当作两档验收。
5. **P1-2 暂缓。** 仅当用户要求处理落地后的 MM 抽搐时，再执行本方案 E 节。

诊断、物理修正、MM 路由、文档状态应分开提交，方便回退和归因。不要对当前大量未提交资产做覆盖式重烘。P0-1 和 P0-2 在修后 BP PIE 录制与自动化都通过之前保持“未修/未验收”。

## A. P1-3：建立可判因的运动交棒记录（也是 P0 前置）

17:53 PIE 完成 CMC 调用边界、`HandleImpact`、GA/Host 交接的验证；19:35 与 20:00 的子阶段探针已把首次写速定位到 `PerformMovement:2870 → ConstrainAnimRootMotionVelocity`。本节下方原始记录点保留作实施记录。**P1-3 的帧内可追溯层已交付**（子阶段记录层 + 遥测列；本轮两次定名都靠它）。仍未定名的是「`RootMotionFromEverything` 下 AnimGraph 侧近零根位移的贡献节点」—— 该项自 20:00 挂账，属判因补充，**不再是 P0-1 的前置**（P0-1 现在的定性是「症状已消、机制未修」，来源已在 §7 定名为 `AnimSync.cpp:98/102` 的权重路径）。

### A1. 最小记录点

- 在 `UMHGZAirDodgeAbility` 的 `HandleAirDodgeMovementFinished`、`HandleAirDodgeVisualCompleted`、`TryFinishAirDodge`、`EndAbility` 进入/离开，以及 `UMHGZWeaponRuntimeHostComponent::BeginAerialFalling` 进入/离开记录事件。还要记录 RMS 安装、到期、清理，和 `HandleLanded` / `PlayAerialLandingVisual` 的写速前后。
- 以 CMC 的实际运动更新为边界，记录 `PerformMovement` **前/后**的胶囊位置、`Velocity`、模式、有效 RMS ID/状态、动画根运动是否参与、`GravityScale`、`BrakingDecelerationFalling`、`FallingLateralFriction`、输入/加速度和碰撞结果。`PerformMovement` 在 UE 5.6 可覆写；若采用项目专用 `UCharacterMovementComponent` 子类，先核对 `BP_IG_Character` 的组件替换兼容性，不能为了监控破坏现有蓝图。帧内更细的 `RootMotionParams` / `PhysFalling` 分支优先在项目 CMC 可覆写方法的前后采样；若仍不能定位，再用引擎 trace/日志或临时断点。不能拿帧末 `HasAnimRootMotion` 推断帧内状态。
- `ShieldAerialAnimRootMotion` **被调用时**记录输入/输出根变换和 CMC 速度；在接缝附近取消目前“仅前 20 次”日志的盲区，改为按会话或短窗口限流。不要常开全程逐帧大日志。
- 使用一个 `AerialMotionSessionID` 贯穿 GA、Task、Host、CMC、Animation 的事件；另写同帧序号、WorldTime、调用阶段、前后 XYZ 速度/位置、源 ID、蒙太奇名/权重、`bRootMotionDisabled`、命中法线。能力结束后由 Host 保留该 ID 至下落/落地结束，不能因 GA token 清除而失联。记录层默认关闭，运行时/PIE 显式打开。

### A2. 如何判读断点

以 17:53 历史录制的 `2595`（以及另 13 个同型断点）为起点：进入 CMC 时 XY 为 900，离开为近零，仍处于 Falling；没有记录到 `HandleImpact`，有效 RMS 为 0，Z 速度按重力连续。下面是当时的判读分支；19:35 和 20:00 的探针已命中第二行，接下来查动画根的贡献来源和安全门控：

| 观测 | 修复指向 |
|---|---|
| `PerformMovement` **前** XY 已从 900 变 0 | 查上一个更新后的 GA/Host/蓝图回调、动画完成/结束清理；在真正的赋值或停移动调用点修复。 |
| 进入 CMC 时 900、出来 0，且动画根分支真的执行 | 查该帧有效动画实例/源、抽取标志和盾的调用顺序；修根运动覆盖的**实际路径**，保留地面合法根运动。不凭资产 `HasRootMotion=0` 推翻帧内证据。 |
| 进入 CMC 时 900、出来 0，动画根分支没执行 | 查 `CalcVelocity`/制动、RMS 清理、模式变更、碰撞 sweep/slide、速度反推；只调整命中的分支或配置。 |
| CMC 前后均为 900，但遥测为 0 | 先修采样时点/对象混淆；用胶囊位置和同帧事件复核，暂不改运动。 |

17:53 的 14 次断点中，JumpForce 最后活跃采样均早于断点 7 帧，速度在这段间隔仍保持 900；源到期不是直接写速帧。`157` 在断点后的交接才起播，也不是直接写速帧。修复仍要维持清晰所有权：JumpForce 退出后由 CMC Falling 延续弹道，空回视觉尾段及 `157` 不重新设水平速度；真实碰撞/落地仍可明确改变速度。诊断不变式可以报警，但不自动补 900。

## B. P0-1：按证据实施修复

**当前状态（24:00 审计）：症状已消、机制未修。** 缝那一帧槽被填满（保持终态的空回淡出 + `157` 淡入）⇒ 下层图不再漏出近零根位移 ⇒ `PerformMovement:2870` 那一帧**不可达**（`20260923-235744`：缝上 `vXY` 7/7 恒 900、下坠段保持 900）。**写点本身没修、窄门控已删** ⇒ 机制在其它动作仍会触发（同录制 `AM_IG_TuCi` 一帧 424 → 0）。20:00 回退版的「13 次无阻挡归零」是**历史读数**，不是当前行为。`PerformMovement:2870 → ConstrainAnimRootMotionVelocity` 仍是被证实的写速链。Host 根盾因「换武器 → `TeardownRuntime` → `RebuildRuntime`」未重绑而失效，这解释了为何既有盾没有拦截；但仅重绑会把其它动作合法的空中根位移/旋转一起替换，19:42 的用户 PIE 已判定不可接受并回退。下一步须确定近零根运动的动画来源及其运动所有权，然后设计窄门控；不能把“重绑旧盾”或“低于 10 cm/s 即替换”写成已验证修法。

当前应优先追查 `RootMotionFromEverything` 下哪个 AnimGraph 节点/序列在淡出帧提供了近零根位移，以及空回尾段的胶囊位移所有权。GA/Host 完成回调晚于断点一帧，不是本批断点的首要写速候选。最终门控落点应由来源与跨动作回归决定，遵循下列约束：

- 将空回尾段非预期的动画根覆盖与其它空中动作合法的根位移/旋转分别验收；近零速度本身不是判定动画根无效的充分条件。保持地面 MM 根运动原样。
- 重绑旧盾只能作为受控诊断对照，不是修复：它曾拦住归零，也曾引入其它动作回归。不能只靠 `ShieldAerialAnimRootMotion` 直接调用测试证明实际运行时的门控安全。
- 维护现有 `MaintainLastRootMotionVelocity` 与源结束后的自由落体连续性；不要把整个 `137` 动画缩短到 `JumpForce` 时长，也不要在 `157` 入场强制覆写 XY。
- 修复后同一会话应出现 `137` 尾帧、视觉实例消失帧、`157` 首帧三处连续的 XY 速度和位移；空回重力仍为约 `-2378 cm/s²`，`MovementMode` 为 Falling，朝向及视觉尾段不被牺牲。

## C. P0-2：让测试覆盖真正失效的帧

**现有测试缺口（24:00 审计，比原文更弱也更具体）：** `AirDodgeToFallHandoffKeepsPlanarVelocityWithoutCollision` 装配是真链路（真 GA / 真蒙太奇 / 真 instrumented CMC / 真配置），但 ①**交接是测试接缝在第 26 帧摆出来的**（`SetAirDodgeExitFlagsForTest` + `HandleAirDodgeVisualCompletedForTest`，正是本方案禁止的形态）；②测试世界**不 tick**、`AnimInstance` 是基础 `UAnimInstance`（无动画图）；③**表现资产的根运动抽取在段级就是关的**（`MHGZStripPresentationRootMotion` + 资产钉 `PresentationClipsHaveNoRootMotion`）⇒ `bHasRootMotion` 恒假、引擎 `CharacterMovementComponent.cpp:2870` 不可达 ⇒ **它在缺陷存在与不存在时都通过**。全仓库没有任何一条会在 P0-1 存在时变红 ⇒ **P0-2 未满足**。

1. 在 `MHGZM5AirDodgeTests.cpp` 增加**实际 tick 的集成测试**：加载生产 `BP_IG_Character` / `DA_IG_Combat` / `AM_IG_AirDodge` / `AM_IG_AerialFall_W`，在有足够净空的地图或测试世界激活 GA，从弹道启动跑到 `157` 已播放至少两帧。不能只构造无 AnimInstance 的 Host harness、直接调用盾、或只查资产标志。
2. 以实际 `MovementMode=Falling`、未触地、无有效外力/碰撞的试次判定；跟踪同一 session 的 `JumpForce` 完成、空回视觉完成、系统下落起播。对每个跨越接缝的更新，要求 `|vXY_after - vXY_before| ≤ 5 cm/s`（或依据真实帧噪声收紧）、方向无突跳；`|ΔXY - ∫vXY dt|` 用本帧实际 `dt` 设容差，不把 40 Hz 的 22.5 cm 固化为所有帧率通用值。`vZ` 按同一重力档连续。
3. 覆盖正常完整空回、源先结束但蒙太奇未结束、尾段提前落地、被其他动作中断四条路径。后三条应各有**自己的**不变式：提前落地按 `148` 规则重设；中断交给新动作所有者，不误报必须维持 900。
4. 测试须可证明旧版本会因接缝归零而失败 —— ⚠ **构造「旧版本红」必须同时钉代码修订与资产修订**（根运动刺激物已从表现资产剥离），否则今天无论如何都红不了；这是 P0-2 未满足的直接原因。修复版跑 `MHGZ.M5`（最新 27/27）及全量 `MHGZ` 自动化。现有 `AnimRootMotionShield`、`PresentationClipsHaveNoRootMotion` 保留作辅助钉，不能用它们的通过数宣称 P0 通过。
5. 最后在真实 PIE 以正常速度和 `slomo 0.1` 复测，至少覆盖两个朝向、无/有白灯、空回从撑杆跳/舞踏可操作窗进入；保存原始 CSV、自动分析结果和用户目视结论。这里是自动化以外的实机验收，不用视频主观判断代替数字判据。

## D. P1-1：落地速度重设的真实时序

**执行状态（2026-09-23 24:00）：部分满足。** 保留认领落地时按方向设为 337 cm/s 的既有逻辑，并在本次 CMC PerformMovement 结束时只补写一次（`PlayAerialLandingVisual` 回调写值 + `QueuePostMovementLandingSpeedReset`/`ApplyPendingLandingSpeedReset`；非每帧锁速）。**两档入速的真触地数据均已取得**：约 900（`20260923-235744` 7/7、`20260923-225916` 11 次）与约 300（`225916` f1441/f3811 = 298.6/296.4、`194246` f2883 = 298.6），写后一律 **337.0**。未闭合的是三条：①自动化仍是「手设速度 + 直接调表现入口」的函数内形态（无真实 `LandedDelegate`）；②「下一帧值」零覆盖；③**「与 Rise `148` 首帧相同采样相位」只有散文** —— 实测落地**首采样帧读到 900、次帧才 337**，而撑杆跳档首帧即 337 ⇒ 相位不定就判不了 `337±6` 指哪一点。

历史上 `PlayAerialLandingVisual()` 的即时写值会被同帧 Walking 制动削弱；已加入一次性 CMC 帧末校正。下面是完整两档入速的验收设计（数据已到手，剩下的只是采样点定义与断言形态，不再需要新试次）。

1. 在 `HandleLanded → PlayAerialLandingVisual` 的写前/写后、CMC 落地物理处理结束、下一帧开始各留一条 session 事件，并记录输入方向、入速和当帧剩余 `dt`；明确 Rise 的 `148` 首帧与项目哪一个采样点对应。
2. 用约 300 和 900 cm/s 两档入速各做多次落地，验证**认领落地表现**才重设到 `337±6`；普通下台阶、提前中断等路径不误触发。近零入速取角色朝向的现有回退逻辑单独测。
3. 若写后确为 337，且项目与 Rise 同相位采样也符合 337，只修遥测/测试口径；若同相位样本被同帧 Walking 制动改坏，才在 CMC/Host 的落地运动阶段修正更新顺序或短暂的运动所有权，并用 Rise `148` 后续速度/位移记录确定持续时间和曲线。**不能**每 Tick 强设 337，也不能让 `AM_IG_AerialLanding` 的姿势根运动与 CMC 同时驱动胶囊。
4. 验收除首帧速度外，还要检查落地到移动/站定的连续位移及可控性；不以“曾有一瞬间是 337”作为最终通过。

## E. P1-2：落地至 MM 的相位连续出口

**状态：未实现（2026-09-23，用户决定推迟）。** 不改 Motion Matching 路由或落地动画出口 ⇒ 本节 1~4 条**零已落地代码**（代码 grep 无 `EMHGZMotionMatchingHandoffType::LandingExit`，`HandleAerialLandingMontageEnded` 不发布任何 MM 载荷，AnimInstance 无落地专用路由）⇒ 断链仍是「落地 → Idle 2~3 帧 → `Walk_Start@0`」。下方保留完整设计与验收依据。恢复条件：仅当用户要求处理落地后的 MM 姿态抽搐时启动。

这项处理的是**落地动作播完后的姿态抽搐**，与 P0 空中 XY 归零分开。现有 `AMHGZCharacter` 在 `IsAerialLanding()` 时令 `bForceMMIdle`，AnimInstance 释放时还有 `bWasForceMMIdle` 测量重置、`MMStartInputSettleRemaining` 等路径；旧观测是 `Landing → Idle 2～3 帧 → Walk_Start@0`。先用最新录制的 MMQuery/Selection 再确认该链仍存在，不假定旧观测必然等于最新版本。

1. 给 `EMHGZMotionMatchingHandoffType` 加 `LandingExit`，携带落地出口的步态泳道/输入快照和 session/serial。**落地不是 GA**：现有 `PublishMotionMatchingHandoff(ActionToken, …)` 明确要求有效且仍活跃的 ActionToken，因此要在 Host 增加**系统拥有的发布入口**或等价独立 payload，不能伪造 GA token。保持“新动作覆盖旧载荷、AnimGraph 消费后按 serial 清除”的规则。
2. `HandleAerialLandingMontageEnded` 只在自然结束、仍在地面、未被新动作接管时发布；输入仍推着时走 Move 路由，输入已松则走 Idle/Stop 路由。落地期间要保留可用于查询的末帧姿态/脚相和 `MM_MoveGait` 泳道；若现有资产没有可靠的脚相标记，先离线提取或加明确的脚相元数据，不能只按速度数值猜左右脚。
3. 在 `UMHGZMotionMatchingAnimInstance` 的出口路由/Chooser 中让 `LandingExit + 持续输入` 的**下一次**搜索只选匹配泳道的 Move loop 候选，并以落地末姿和脚相选择进入时间，跳过 Idle 和 `Walk_Start@0`。真正从站定开始走仍走原有 `Walk_Start`。只对该 handoff 关闭/绕过会造成 2～3 帧空档的 `MMForceIdleReleaseHoldRemaining`、`MMStartInputSettleRemaining` 或动作 Idle 路由；不全局关闭这些机制。物理输入和 MM 姿态选择分离，落地期间不能让底层 loop 根运动提前推动胶囊。
4. 验收记录落地蒙太奇末帧、首个 MM 选中序列和时间、`MM_MoveGait` 差值、左右脚位置/速度、同帧胶囊位移。持续推杆：无 Idle 插帧、无 `Walk_Start@0`、脚相匹配且 `MoveGaitDelta` 接近 0；松杆：正常 Idle；落地瞬间改向：方向正确。正常速度和 `slomo 0.1` 均无肉眼可见抽搐。若姿态连续但胶囊仍顿，回到 D 的物理时序查，不继续调蒙太奇 Blend 数值。

## 最终验收矩阵与止损规则

| 场景 | 必须通过的检查 |
|---|---|
| 空回跑满后 `137 → 157` | 单一 session 的速度/位移/重力连续；无 `900→0` 帧；下落蒙太奇不新增运动所有者。 |
| 空回尾段提前落地 | 只发生一次落地认领；`148` 重设与表现开始同相位；不会先进入 `157` 再重复落地。 |
| 空回被新空中动作接管 | 原 session 清理完整，新动作成为唯一位移 owner；不误维持旧的 900。 |
| 有/无白灯、不同朝向、40/60/120 Hz 或可用的固定步长 | 接缝不依赖特定采样间隔，位移判据按实际 `dt`；真值允许范围逐项记录。 |
| 持续推杆落地、松杆落地、落地时改向 | 物理速度和姿态出口分别合格；移动立刻有位移且没有两次硬切。 |
| 四向撑杆跳与现有地面 MM | 没有因公共 CMC/根运动门控修改出现新停顿、错误朝向或地面根运动失效。 |

完成条件：冷启动 Development Editor 编译、实际测试数量及结果、资产验证、至少一份修后 PIE 原始录制和与失败基线的对比、用户视觉确认**全部齐全**。任一门槛不达标则保持“未修”，附上复现 session 和当前已排除的分支。

⚠ **状态口径（2026-09-23 24:00）**：「未修」现在只对 **P0-1 的机制**与 **P0-2** 成立（P0-1 症状已消、P1-1 部分满足、P1-2 已推迟、P1-3 已交付），不要对整张表套用一个标签。**P0-2 的红线自动化须先钉代码修订 + 资产修订**，否则该门槛不得作为其它条目的阻塞项。不得用通过静态测试、调长混入或把 `157` 首帧强行补速代替因果修复。
