# 空回坠落顿挫与当前空中系统审查

> 2026-09-23，根据回退修复、保留探针后的 PIE session `20260923-200039-BP_IG_Character_C_0-55482` 写就。
> 范围是空回到系统下落/落地的运动交接。17:53 等较早录制保留作历史基线。
>
> ⚠ **本文是 20:00 回退版的快照（历史基线），不再是当前结论。** 后续同一晚的结论请以
> `空回P0-P1完整修复方案.md` 的「执行进度 → 24:00 审计表」与 `空中下落实现缺口.md` §6.5/§6.5.1/§7 为准：
> 缝上 `900→0` 的**症状已消**（`20260923-235744`：7/7 恒 900、下坠段保持 900，槽被填满后写点那帧不可达），
> 但**写点 `PerformMovement:2870` 未修**、机制在其它动作仍触发（同录制 `AM_IG_TuCi` 一帧 424 → 0）。
> 下表各条已就地回填现状。

## 结论

**（历史：20:00 回退版。当前状态：症状已消、机制未修 —— 见文首指针。）** 20:00 回退版录制中，13 次无阻挡的完整空回都在蒙太奇淡出约 `1.174～1.183 s` 时，由 CMC 一次 `PerformMovement` 将水平速度从 `900` 降至 `0～0.391 cm/s`。子阶段探针已定名写手：`ConstrainAnimRootMotionVelocity` 在 `PerformMovement:2870` 的动画根分支，用近零的 `AnimRootMotionVelocity` 覆盖了原有 XY；当帧 `RootMotionShieldBound=0`、无有效 RootMotionSource、无 `HandleImpact`。速度丢失早于 GA 完成和 Host 系统下落接管。此前 17:53 的 14 次同型断点仍是独立复现证据。

用户目测像是水平和垂直速度同时停顿。20:00 断点帧的胶囊数据仍只支持**水平停顿**：`VelocityZ` 从约 `-1507～-1511` 变为 `-1566～-1572 cm/s`，继续按重力加速下落。画面上的竖直顿感仍需另查姿势/镜头时序，不能写成已测得的胶囊垂直速度损失。

**写速函数已定位，近零根运动的动画来源在当时尚未定位（后已定名：来源 = `AnimSync.cpp:98/102` 的同步组权重路径，见 `空中下落实现缺口.md` §7 的 21:10 条目 —— 不是某个动画图节点）。** UE 5.6 的 `ConstrainAnimRootMotionVelocity` 在 Falling 保留 Z、替换 XY；20:00 的函数输入/输出已直接证实它发生了。空回蒙太奇报告 `MontageHasRootMotion=0`，帧末动画代理也报告 `ProxyHasRootMotion=0`，但同帧 CMC 内 `RootMotionParams.bHasRootMotion=1`。该 AnimInstance 的 `AnimRootMotionMode=2`（UE 枚举为 `RootMotionFromEverything`），因此不能凭蒙太奇标志认定近零根来自空回蒙太奇本身；须追踪动画图中贡献根运动的节点/序列。

## 20:00 回退版录制：新探针的判因结果

原始数据：`Saved/RuntimeTelemetry/20260923-200039-BP_IG_Character_C_0-55482/Character/MovementPhases.csv`，同会话 `Animation/MontageInstances.csv`、`AnimGraphRootMotion.csv` 及 `Saved/Logs/MHGZ.log`。13 个无阻挡断点为帧 `679、850、1039、1266、1962、2140、2348、2556、2758、3018、3239、3436、3644`；每次 `CMC.PerformMovement.Pre` 的 XY 都是 `900 cm/s`，`Post` 为 `0～0.391 cm/s`，且根盾标志均为 0。整份 `MovementPhases.csv` 的 34334 条样本中，`RootMotionShieldBound` 全为 0；日志只有一次 `bound`，紧随一次 `unbound`。当前 `RebuildRuntime` 没有重绑，符合用户说明的回退状态。

以帧 `679` 为例：`CMC.HandlePendingLaunch.Post` 的速度仍为 `(-143.1962,-888.5366,-1506.6844)`；随后 `CMC.ConstrainAnimRootMotionVelocity` 记录 `RootMotionParams` 的 XY 位移约 `(0.0009,0.0058) cm`，算出的动画根速度 `(0.0373,0.2314) cm/s`，函数输出也为这个 XY；下一条 `CMC.StartNewPhysics.Pre` 已变为 `(0.0373,0.2314,-1506.6844)`。该帧无 `HandleImpact`，空回蒙太奇 `Position=1.1752 s`、`Weight=0.2442`、`MontageHasRootMotion=0`。其余 12 次均在相同函数边界复现。

19:42 曾尝试在 `RebuildRuntime` 重绑既有根盾；其 PIE 没出现这类 `900→近零`，但用户观察到其它动作回归，已撤回行为改动，保留探针。因此“重绑能止本症状”不等于“原盾逻辑可直接投入使用”：它在空中/托管窗口会把**所有**动画根位移换成 `Velocity*Δt`，同时清掉根旋转。方案文档的近零速度阈值也尚未验证合法动作的静止帧、方向变化和源交接，不能当作已确认修法。

## 17:53 录制的断点证据（历史基线）

原始数据：`Saved/RuntimeTelemetry/20260923-175313-BP_IG_Character_C_0-55482/` 的 `Character/MovementPhases.csv`、`RootMotionSources.csv`、`Spatial.csv`，`Animation/MontageInstances.csv`、`AnimGraphRootMotion.csv`，以及 `Saved/Logs/MHGZ.log`。以同一次 CMC 调用的 `PerformMovement.Pre/Post` 配对；跨 CSV 时核对 `WorldTimeSeconds`，不能只凭相同的 `Frame` 数字推定采样先后。

14 个断点帧为 `2595、2805、3182、3424、3630、3826、4025、4215、4405、4602、4806、5000、5192、5456`。每次空回 JumpForce 的最后活跃采样均早于断点 7 帧。断点时空回蒙太奇已停止并淡出到 `Position=1.1744～1.1768 s`、`Weight=0.2094～0.2688`；下一 CMC 帧才触发 `AirDodge.VisualCompleted → EndAbility → Host.BeginAerialFalling`。这说明能力清理和系统下落接棒发生在速度丢失之后。

| 14 次断点的同帧 CMC 对照 | 实测范围 | 含义 |
|---|---:|---|
| `|vXY|` 前 → 后 | `900.0 → 0.04～0.38 cm/s` | 水平速度和水平胶囊位移一起消失 |
| 胶囊水平位移 | `0.001～0.009 cm` | 正常按约 `0.025 s` 和 900 cm/s 应约 22.5 cm |
| `VelocityZ` 前 → 后 | 约 `-1506～-1510 → -1566～-1570 cm/s` | 继续按约 `-2376 cm/s²` 加速下落 |
| 胶囊垂直位移 | `-37.32～-39.05 cm` | 与前后 Z 速度的梯形积分吻合 |
| CMC `HandleImpact` / 有效 RMS | `0 / 0`，14 次均如此 | 本批断点无记录到的阻挡响应，位移源已退出 |

例：帧 `2595`，`Δt=0.0251 s`。CMC 前 `Velocity=(881.31,-182.46,-1508.87)`，后为 `(-0.30,0.06,-1568.44) cm/s`；XY 位移约 `0.008 cm`，Z 位移 `-38.58 cm`（按前后 Z 速度预计 `-38.62 cm`）。该帧无 `HandleImpact`。同会话的帧 `2527`、`3113` 撞墙时记录了阻挡对象和法线，证明新命中列确实在工作。

落地速度另有阶段性结果：14 次 `CMC.LandingSpeedReset.Applied` 之后，同次 `PerformMovement.Post` 均为 `337 cm/s`；但这 14 次触地入速都仅约 `0～0.2 cm/s`，因为此前空回已丢失水平速度。它验证了一次性帧末赋值在 PIE 执行，尚不能代替约 300/900 cm/s 两档入速验收。

## 16:37 录制与当时的中间判断（已被新录制修正）

当时的 session 是 `Saved/RuntimeTelemetry/20260923-163739-BP_IG_Character_C_0-55480/`。其中 11 个高水平速度跌落里，9 个发生在空回蒙太奇淡出附近的 CMC 更新中，2 个对应角色贴近墙面时的接触；首次一例在跌落约 3 帧后才出现训练木桩的组件命中通知。旧版 `CapsuleHits.csv` 只记录组件命中回调，不等价于 CMC sweep 的精确阻挡记录，因此“当帧没有 CapsuleHits”不能排除碰撞响应。

后续代码新增 `CMC.HandleImpact.Pre/Post` 记录，含阻挡 actor/component、命中法线、move delta 与 time slice；同时保留同帧 `CMC.PerformMovement.Pre/Post`。当时测试角色上的 GA/蒙太奇/CMC 无碰撞回归保持约 900 cm/s，因此曾把场景碰撞列为主要怀疑。但该自动化手动触发视觉完成，没有跑实际 BP 动画淡出的断点帧；上方 17:53 PIE 录制已否定“本批顿挫更像碰撞”的推断。

同一轮还补上落地速度校正：`Landed` 回调中的 337 cm/s 曾在当帧 Walking 更新后降至约 197，下一帧约 68；新增的一次性 CMC 更新末校正已在 17:53 PIE 的近零入速触地中运行。两档入速确认仍待进行。落地动画结束后的 MM 姿态抽搐按用户要求暂不修。

## 早期基线证据与口径

- 初始主录制：`Saved/RuntimeTelemetry/20260923-151654-BP_IG_Character_C_0-55480/`，取其中 `Character/{Spatial,State,RootMotionSources,CapsuleHits}.csv` 和 `Animation/{MontageInstances,Playback}.csv`。
- 交叉复核：`20260923-141138-BP_IG_Character_C_0-55478` 中 13 次空回，除 1 次提前落地外，其余 12 次也在相同的蒙太奇收尾边界出现约 `900 → 0 cm/s`。这些早期录制是在表现序列剥离根运动、Host 根运动盾接线之后取得的。
- 单位：UE 空间是 `cm` / `cm/s`，主录制采样间隔约 `0.025 s`。`Spatial.AccelerationX/Y/Z` 是**相邻速度样本的差分**，不能解释成施加给 CMC 的力或输入加速度。`Spatial.HasAnimRootMotion` 是在 `PerformMovement` 之外采样的状态，也不能单独用于判定帧内是否提取了动画根运动。
- 对照真值：`docs/reference/下落物理.md` 的 `137 → 157` 应是一条连续抛物线，水平速度在下落段不应重置；落地 `148` 才是速度重置点。

### 七次完整空回的断点

| 归零帧 | 前帧 XY 速度 | 本帧 XY 速度 | 本帧实际 XY 位移 | 按前帧速度预计位移 | 本帧胶囊命中记录 |
|---:|---:|---:|---:|---:|---:|
| 5664 | 900.0 | 0.3 | 0.01 cm | 22.63 cm | 0 |
| 5907 | 900.0 | 0.0 | 0.00 cm | 22.44 cm | 0 |
| 6128 | 900.0 | 0.2 | 0.00 cm | 22.71 cm | 0 |
| 6410 | 900.0 | 0.1 | 0.00 cm | 22.65 cm | 0 |
| 6689 | 900.0 | 0.1 | 0.00 cm | 22.46 cm | 0 |
| 7138 | 900.0 | 0.3 | 0.01 cm | 22.55 cm | 0 |
| 7409 | 900.0 | 0.3 | 0.01 cm | 23.03 cm | 0 |

例：`5663 → 5664` 的 `VelocityZ` 为 `-1507.4 → -1566.8 cm/s`，约 `0.0251 s` 内减少 `59.4 cm/s`，与下落重力吻合；同帧 `VelocityXY` 为 `900.0 → 0.3 cm/s`。位置列也显示胶囊的水平位移只有约 `0.008 cm`。因此不是单一 `Velocity` 读数失真。

### 首次复现的状态时间线

| 帧 | 空回位移源 | 空回蒙太奇 | 系统下落蒙太奇 | XY 速度 | CMC 模式 |
|---:|---|---|---|---:|---|
| 5656 | `JumpForce` 尚在，时间 `0.9759 / 0.9949 s` | 播放 | 未播 | 900.0 | Falling |
| 5658–5661 | 已无活跃源 | 继续播放 | 未播 | 900.0 | Falling |
| 5662–5663 | 已无活跃源 | 停止并淡出，权重 `1.0 → 0.2315` | 未播 | 900.0 | Falling |
| **5664** | 无 | **实例已消失** | **未播** | **0.3** | Falling |
| 5665 | 无 | 无 | `AM_IG_AerialFall_W` 起播 | 0.1 | Falling |

这一时间线排除了“位移源在 `0.995 s` 结束时直接清零”作为直接解释：源消失后还连续走了约 6 帧、水平速度仍为 900。它也不支持把 **下落蒙太奇首帧**当作归零点：归零发生在下落蒙太奇起播之前一帧。`State.csv` 在 `5663–5664` 均显示 `BlockMovement=1`、`ForceMMIdle=1`，并非这两个开关恰在断点翻转。七次归零帧都没有 `CapsuleHits.csv` 事件，且发生地点不同；碰撞不是目前最有力的解释，但 CSV 的命中记录尚不足以排除 CMC 内部的所有 sweep/slide 分支。

## 早期根运动证据的误读与复核

当前 `AM_IG_AirDodge` 和 `AM_IG_AerialFall_W` 的蒙太奇 `HasRootMotion` 在遥测里为 `0`；后者实例另有 `RootMotionDisabled=1`。`Saved/_strip_root_motion_report.json` 也报告这些表现蒙太奇没有根运动、相关序列关闭抽取。Host 的 `ShieldAerialAnimRootMotion` 已绑定到 CMC 的 `ProcessRootMotionPostConvertToWorld`，但早期 `MHGZ.log` 只见绑定日志，没有看到该函数的 `shielding anim root` 日志。该日志有前 20 次上限，所以最后一项只是线索，不单独作为排除证据。

代码里的盾和序列清理防止了某些动画根运动覆盖路径，但 17:53 录制仍 14/14 复现。`AnimRootMotionShield` 和 `PresentationClipsHaveNoRootMotion` 只测防护/资产标志；新增的 `AirDodgeToFallHandoffKeepsPlanarVelocityWithoutCollision` 虽使用真实 GA 与资产，却在测试角色上手动触发视觉完成，跳过了实际 BP 动画淡出到 `1.175 s` 的帧。因此其通过不构成这次故障的回归通过。早期 `ProxyHasRootMotion=0`、蒙太奇 `MontageHasRootMotion=0`、根运动盾无回调曾使动画根分支被错误排除；20:00 的 CMC 帧内探针已证实该分支执行，而且盾当时未绑定。

## 当前不合理或尚未闭环的地方

| 优先级 | 问题 | 依据与影响 |
|---|---|---|
| P0-1 | 空回淡出阶段 CMC 用近零动画根速度覆盖水平速度 | **24:00 回填：症状已消、机制未修。** 20:00 回退版 13 次无阻挡断点在 `PerformMovement:2870 → ConstrainAnimRootMotionVelocity`；但改后缝那一帧槽被填满（§6.5 的撑杆跳配方）⇒ 该帧不可达 ⇒ `20260923-235744` 缝上 7/7 恒 900、下坠段保持 900。**写点未动**；机制在别处仍触发（同录制 `AM_IG_TuCi` 一帧 424→0）。近零根来源已定名（`AnimSync.cpp:98/102`），重绑旧盾仍是「能止症但打坏合法空中根运动」的已否决路线。 |
| P0-2 | 自动化没有运行出错的 BP 蒙太奇淡出帧 | **24:00 回填：未满足。** 除「手动结束蒙太奇 + 世界不 tick + 基础 `UAnimInstance` 无动画图」外，还有一条更硬：**表现资产的根运动抽取在段级就是关的**（`MHGZStripPresentationRootMotion` + 资产钉）⇒ `bHasRootMotion` 恒假 ⇒ 引擎 `CharacterMovementComponent.cpp:2870` 不可达 ⇒ 该测试在缺陷有无时都通过；全仓库无一条会在 P0-1 存在时变红。⚠ 构造「旧版本红」须**同时钉代码修订与资产修订**。 |
| P1-1 | 落地速度重设仍缺不同入速的 PIE 验收 | **24:00 回填：部分满足。** 两档入速的真触地数据已取得（约 900：`235744` 7/7、`225916` 11 次；约 300：`225916` f1441/f3811 = 298.6/296.4），写后均 337.0。**仍未闭合**：真 `LandedDelegate` 的逐帧断言、「下一帧值」、以及「与 Rise 148 首帧相同采样相位」落成判据（实测首采样帧 900、次帧 337；撑杆跳档首帧即 337）。 |
| P1-2 | 落地后 Motion Matching 出口仍有 Idle/起步相位断链（**24:00 回填：未实现，用户决定推迟；代码 grep 无 `LandingExit` 载荷与落地专用 MM 路由**） | `m5-plan.md` §6.1 记录 `Landing → Idle 2～3 帧 → Walk_Start@0`；`AMHGZCharacter::Tick/DoMove` 在 AerialLanding 时强制 `bForceMMIdle`，收尾仍可能造成用户之前目测的抽搐。它与本次**空中速度归零**是两个问题，修复时不要用同一症状名混在一起。 |
| P1-3 | 运动交棒探针已覆盖本断点，动画根来源仍不可追溯 | **24:00 回填：本条主体已交付**（子阶段记录层 + 遥测列；本轮两次定名都靠它：task 的 tick 门、缝上的钳制帧）。**残留子项**：AnimGraph 侧近零根位移的贡献节点仍未定名 —— 属判因补充，不再是 P0-1 的前置（来源已定名为 `AnimSync` 权重路径）。 |
| P2 | 部分阶段文档仍有历史状态 | **24:00 回填：已按现状清理一轮** —— 方案文档表加了状态列、缺口 §6.5/§7 就地回填、本文降级为历史基线、`m5-plan.md` 的过期开关与计数同步；脚本生成的 `docs/reference/下落物理.md` §六末尾仍待按验收结果审视（改生成器模板）。 |
| P2 | M5 仍有独立未完成项 | `m5-plan.md` §6/§7 已列出撑杆跳起手段速度下陷、弧尾部分帧位移损失、若干方向真值偏差及低样本组。这些是待核对/待验收项；不能因空回弹道主要标量接近 Rise 就把整个空中系统视为完成。 |

上述当前结论针对 20:00 回退版的 13 次无阻挡空回、19:42 失败尝试、17:53 历史基线及所列 UE 5.6 / 项目源码。四向撑杆跳的新资产与真值尚未逐组重算，所以不把历史百分比当作最新版本的误差。

## 下一轮最小定位方案（本文不实施）

1. ✅ **已完成（21:10）**：保留现有 CMC 探针，补一条从 AnimGraph 根运动提取到 CMC `RootMotionParams` 的来源追踪，确定空回淡出帧究竟哪个节点/序列贡献了近零根位移；同时抽样合法空中动作的低速、停顿、转向帧。`RootMotionMode=RootMotionFromEverything`、蒙太奇 `HasRootMotion=0` 并不能单独定名来源。
2. ⛔ **已作废**：先按**动作所有权/阶段**设计窄门控，再考虑是否需要位移大小阈值。（窄门控曾实现并做过 A/B，随后按用户要求整体删除；现行做法是表现交棒配方。）必须保留其他空中动作合法的根位移与根旋转；不能单凭 `|Δroot|/Δt < 10 cm/s` 判断根运动无效，也不能把整个空中阶段改为旧盾的一律替换。
3. ⬜ **仍未做**（= P0-2）：用真实 `BP_IG_Character` 和 AnimInstance 自然推进空回淡出，不手动调用视觉完成回调。修复后连续断言淡出帧、实例消失帧和下落首帧的 XY 速度/胶囊位移，Z 速度与重力积分也要连续；再复测空回之外的撑杆跳、空中攻击、舞踏、落地表现和墙面碰撞，确认没有 19:42 那类回归。
4. ✅ **已按此定位**（姿势层空档 = §6.5；改后录制的 `MeshBoundsZ − LocationZ` 已逐帧变化）。原文：若胶囊 Z 已连续而画面仍像竖直停顿，单独比较淡出尾帧、无蒙太奇帧与下落入场帧的 pelvis/脚和镜头位置；不能用修改弹道重力去处理纯姿势或镜头接缝。

**24:00 回填**：P0-1 的写入函数已找到、**写点仍未修**；缝上症状已由表现交棒配方消掉，但机制在别处仍触发。不要在 `157` 起播时强写 900 来掩盖 `137` 淡出帧的改写 —— 这条不变。若日后要动写点，先按 `空回P0-P1完整修复方案.md` 的 24:00 审计定义「旧版本红」所需的代码/资产修订，并把 P1-1 的采样相位先定下来。
