# 空中下落物理（生成物，勿手改）

> 由 `Scripts/MHRise/build_fall_physics.py` 从原始实录生成。**改数请改脚本或原始录制。**

**真值源**：`C:\apps\steam\steamapps\common\MonsterHunterRise\reframework\data\MHGZ_AerialTrajectoryRecorder` —— 全库扫描，老格式跳过。

本表回答四件事：每个空中状态的重力是多少、下落段的重力从哪来、落地那一刻做了什么、水平速度在下落期间衰不衰减。**重力都以 `g₀ = 980 cm/s²` 为单位。**

## 一、结论：重力**按状态逐条不同**，不是全局一个 g

**方法**：对每一段的 `velocity_y` 做最小二乘求斜率（**不用逐帧差分** —— 位置抖动会被二阶差分放大，实测 a_y 在 ±8000 cm/s² 之间乱跳）。**掐掉末 3 帧**（落地刹车会把 g 系统性拉浅：`143` 全段拟合 −1653，掐掉后 −2386）**且只取离地帧**（`world_y > 60 cm`）。

**R² 是「这段到底是不是一条抛物线」的判据** —— 两段式动作（`187` 发射相 + 滑行相）单直线拟合的 R² 会掉到 0.3~0.9，那种 id **不给单一 g**。

| id | 招式 | 可拟合段 | 其中可信 | **g 中位 cm/s²** | **÷g₀** | IQR | R² 中位 | 判定 |
|---|---|---:|---:|---:|---:|---|---:|---|
| `137` | 空中回避 | 220 | 220 | **-2378** | -2.43 | -2381~-2377 | 0.999 | 恒定重力抛物线 |
| `154` | 突进回旋斩进入舞踏 | 74 | 44 | **-1803** | -1.84 | -1804~-1798 | 0.974 | 恒定重力抛物线 |
| `15` | 翻滚后反向移动 | 67 | 67 | **-1586** | -1.62 | -1588~-1584 | 0.999 | 恒定重力抛物线 |
| `142` | 向前起跳后段 | 198 | 151 | **-2397** | -2.45 | -2400~-2395 | 0.996 | 恒定重力抛物线 |
| `156` | 向前起跳后段（白灯） | 187 | 163 | **-2517** | -2.57 | -2521~-2499 | 0.997 | 恒定重力抛物线 |
| `187` | 操虫斩前冲动作 | 122 | 0 | **-1196** | -1.22 | -15957~8553 | 0.369 | **非单一抛物线**（两段式或多相） |
| `199` | 绝虫击突进飞行 | 35 | 13 | **-1188** | -1.21 | -1193~-1186 | 0.933 | **非单一抛物线**（两段式或多相） |
| `143` | 起跳下坠 | 158 | 89 | **-2386** | -2.43 | -2405~-2365 | 0.958 | 恒定重力抛物线 |
| `157` | 起跳下坠（白灯） | 90 | 16 | **-1807** | -1.84 | -1838~-1779 | 0.829 | **非单一抛物线**（两段式或多相） |
| `188` | 操虫斩未击中 | 61 | 55 | **-3101** | -3.16 | -3213~-3096 | 0.991 | 恒定重力抛物线 |
| `30` | （未核对） | 15 | 9 | **-1581** | -1.61 | -1591~-1577 | 0.969 | 恒定重力抛物线 |
| `33` | （未核对） | 1 | 0 | **-1490** | -1.52 | -1490~-1490 | 0.817 | **非单一抛物线**（两段式或多相） |
| `206` | 猎虫滑翔命中以及觉虫击未命中的下坠段 | 28 | 27 | **-2880** | -2.94 | -2882~-2877 | 0.976 | 恒定重力抛物线 |
| `148` | 起跳落地 | 39 | 0 | **-0** | -0.00 | -18~71 | 0.116 | **地面态**（不自由落体） |
| `182` | 铁虫丝跳跃/操虫斩落地 | 11 | 0 | **-59** | -0.06 | -70~-4 | 0.167 | **地面态**（不自由落体） |

**项目侧对照**（`InsectGlaiveCombatConfig.h`，两个常数都是从实录反推的）：
- `AerialFallGravityScale = 2.4246` → **2376 cm/s²**
- `WhiteAerialFallGravityScale = 2.574` → **2523 cm/s²**
- 另有 `BrakingDecelerationFalling = 0.0`、`AerialFallMaxSeconds = 4.0`（**只是看门狗**，不是物理钳制）、全工程**没有** `MaxFallSpeed` 赋值。

## 二、下落段**不设自己的重力**，它继承发起那个动作的

`157` 是这条结论的关键证据：它同一个 id，重力却**随前驱分家**。
混在一起取中位得到的数没有意义，必须分层看：

| `157` 的前驱 | 段数 | g 中位 | ÷g₀ | IQR | 入口 vy 中位 cm/s |
|---|---:|---:|---:|---|---:|
| `189` 操虫斩/觉虫击命中进入舞踏 | 30 | **-1804** | -1.84 | -1838~-1763 | -1038 |
| `137` 空中回避 | 27 | **-2430** | -2.48 | -2568~-2311 | -1636 |
| `156` 向前起跳后段（白灯） | 8 | **-2605** | -2.66 | -2638~-2441 | -2039 |

⇒ **`157` 跟着前驱的重力走**：回避后是 `137` 那一档，舞踏后是 `154` 那一档。所以实现上**下落段什么都不该设** —— 不要给它单独配重力。

> ⚠ 上表两组都**比前驱本身略深约 2%**（`137` −2430 vs `137` 自身 −2378）—— 这是**短段偏差**，不是真的换了重力：`157` 段中位只有 9~13 帧，回归窗口太短，且 id 切换那一帧常带一个小台阶。**判「是不是同一条抛物线」要用第四节的整体二次拟合，不要用这张表的单段斜率。**

## 三、`137` 空中回避 = 一个纯抛体

- g = **-2378 cm/s²**（-2.43 g₀），**R² 中位 0.9991**，220/220 段 R²>0.98，IQR **-2381~-2377**（四分位间距只有几 cm/s² —— 机器精度级别的一致）
- 对照 `AerialFallGravityScale × g₀ = 2376` ⇒ **差 2 cm/s²（0.08%）**

## 四、`137 → 157 → 落地` 是**同一条抛物线**

把 `137`（142 帧）+ 紧跟的 `157` + `148` 前 6 帧拼成**一条**轨迹做二次拟合：

| 量 | 值 |
|---|---:|
| 例数 | 152 |
| **R² > 0.99 的比例** | **152/152** |
| R² 中位 | **0.9970** |
| 拟合加速度中位 | **-2302 cm/s²**（-2.35 g₀）|

⇒ **`157` 处没有任何速度重置** —— id 换了、动画换了，弹道还是那一条。（拟合值比 `137` 单段略浅，是因为尾巴带进了落地刹车的前几帧。）
**所以「下降」不需要单独实现：它是上一条弹道的延续。**

## 五、水平速度：**下坠段全程不衰减**（`188` 是唯一例外）

判据用**逐段比值**（末帧 ÷ 首帧），不用「首帧中位 ÷ 末帧中位」—— 后者会被长段与短段的
组成差异污染。**只列下坠类 id**：`142`/`156` 是撑杆跳的弧段（本身在减速，不是下落），
`187`/`15` 是上抛段，都不属于这一节。

| 下坠 id | 段数 | 首帧中位 cm/s | 末帧中位 cm/s | **逐段比值中位** | 落在 ±5% 内 | 判定 |
|---|---:|---:|---:|---:|---:|---|
| `143` 起跳下坠 | 161 | 299 | 307 | **1.031** | 60/161 | **恒定**（不衰减） |
| `157` 起跳下坠（白灯） | 145 | 890 | 872 | **1.042** | 44/145 | **恒定**（不衰减） |
| `188` 操虫斩未击中 | 63 | 1627 | 1268 | **0.859** | 10/63 | **水平衰减约 20%**（唯一例外，原因未查清） |
| `30` （未核对） | 17 | 480 | 486 | **1.011** | 8/17 | **恒定**（不衰减） |

⇒ `BrakingDecelerationFalling = 0.0` 对**下坠段**是对的：水平速度全程恒定，惯性一点不该衰减。`188`（操虫斩落空后的下坠）是唯一例外。

## 六、落地：**重设**水平速度，不是对入速做摩擦衰减

`148 起跳落地` 首帧的水平速度，按前驱分组：

| `148` 的前驱 | n | 入速中位 cm/s | **`148` 首帧中位** | 比值 | `148` 首帧 IQR |
|---|---:|---:|---:|---:|---|
| `157` 起跳下坠（白灯） | 231 | 859 | **336** | 0.392 | 327~342 |
| `143` 起跳下坠 | 136 | 314 | **338** | 1.077 | 332~341 |
| `156` 向前起跳后段（白灯） | 45 | 306 | **336** | 1.101 | 327~341 |
| `159` 向后起跳后段（白灯） | 38 | 306 | **336** | 1.100 | 332~343 |
| `137` 空中回避 | 33 | 898 | **338** | 0.376 | 335~343 |

**入速 306 也变 337、入速 898 也变 337** ⇒ `148` 首帧恒在 **337 ± 6 cm/s**，**与入速无关**（入速↔落地首帧的相关系数 r = +0.087）。

**这是「重设」而不是「摩擦衰减」的判据**：越是高入速、越该被砍得狠；而实测两头的落点相同。⚠ 项目当前实现是摩擦衰减（引擎侧遥测：落地帧 v2D 292 → 126 → 24 → 0，4 帧到静），**没有这个重设**，见 `docs/using/空中下落实现缺口.md`。
